# 选课系统设计与开发任务计划

> 2026-09-06 执行更新：用户已授权并执行开发环境安装。工具链和隔离构建证据见 [P-02 环境记录](docs/verification/P-02.md)，使用步骤见 [开发环境说明](docs/development-environment.md)。WSL/Docker 已安装，Windows 组件等待重启后验证 MySQL；P-02 继续进行中，P-03 及业务任务状态不变。以下“本轮只同步文档”等描述指 2026-09-04 的历史交付。

> 文档版本：1.0.6  
> 日期：2026-09-04  
> 来源：[requirement.md](requirement.md) v1.0.6，正式业务基线  
> 状态：P-01 已完成；P-02 技术文档已交付、环境验证未完成；P-03 交互文档已交付、静态走查完成、视觉样张验证未完成；其余任务按依赖执行  
> 本轮只同步文档，不创建工单或执行工程代码、迁移、部署。

## 1. 执行原则

用户已经采纳全部业务默认规则，计划不再安排重复的业务确认。按 SDD 顺序执行：正式需求 → 技术与交互设计 → 有依赖的业务任务 → 验收证据。技术设计尚未完成，不能把当前任务描述误认为已经存在的接口和数据库实现。

本计划使用 plan 技能组织任务。每项实现任务交付完整可观察的业务结果，包含所需页面、接口、数据和证明，目标是一轮独立执行及一份聚焦的变更评审或 PR。设计任务只交付设计及必要可行性证据。用户已明确要求本地计划文件，因此将计划保存在本文件。

保留旧 P-01～P-10 的主题以便追踪，新增 P-11～P-20。下表按可执行依赖排序，不按编号大小排序。不得把另一个任务定义的共享接口、错误、权限或一致性规则重新设计一遍。

### 1.1 开发准入

- 业务需求以 requirement.md v1.0.6 为准，P-01 已由本轮完成，不存在需要用户重新选择的默认规则。
- P-02 负责全部共享技术契约和兼容性验证，P-03 负责交互；两者交付相互一致的版本后，P-04 才能开始业务实现。
- 设计产物固定版本，并记录引用的需求版本。实现代理先读需求、对应设计和本任务，再执行；没有相关设计不得自行推测实现选择。
- 技术版本若有实际不可获取或不兼容问题，P-02 应记录证据、提出最小变更并更新正式约束；其他任务只在受影响部分等待解决，不重新打开已采纳的业务决策。
- 原计划中“不包含管理员、学期、退换课或学生名单”的排除项全部失效；每个新任务的“不包含”仅界定该任务分工，不缩减产品范围。
- P-01 为本轮唯一已完成任务。设计、工程脚本、运行环境、测试与部署均未在本轮执行。

### 1.2 依赖与交付顺序

| 顺序 | 任务 | 依赖 | 交付结果 |
| --- | --- | --- | --- |
| 1 | P-01 固定首版业务规则和边界 | 无 | 正式需求与同步计划（已完成） |
| 2 | P-02 固定可实施的技术及验证契约 | P-01 | 技术与验证契约 |
| 3 | P-03 固定表单、列表与周课表交互 | P-01 | 页面及状态设计 |
| 4 | P-04 完成三类账号注册登录及隔离 | P-02、P-03 | 完成三类账号注册登录及隔离 |
| 5 | P-11 按固定日期生成学期并支持切换 | P-04 | 按固定日期生成学期并支持切换 |
| 6 | P-12 让管理员新增查询并编辑师生账号 | P-04 | 让管理员新增查询并编辑师生账号 |
| 7 | P-05 完成合法课程的可靠创建 | P-11 | 完成合法课程的可靠创建 |
| 8 | P-06 完成教师本人课程查询 | P-05 | 完成教师本人课程查询 |
| 9 | P-13 上架和下架本人课程 | P-06 | 上架和下架本人课程 |
| 10 | P-07 完成学生可选课程浏览 | P-13 | 完成学生可选课程浏览 |
| 11 | P-08 完成无冲突且不超额的选课 | P-07 | 完成无冲突且不超额的选课 |
| 12 | P-14 编辑课程内容并保护已有选课 | P-08 | 编辑课程内容并保护已有选课 |
| 13 | P-15 删除无有效选课的本人课程 | P-08 | 删除无有效选课的本人课程 |
| 14 | P-16 退课并允许按规则重新选入 | P-08 | 退课并允许按规则重新选入 |
| 15 | P-17 原子换课并在失败时保留来源 | P-16 | 原子换课并在失败时保留来源 |
| 16 | P-18 删除师生账号并保持关联数据一致 | P-12、P-14、P-15、P-17 | 删除师生账号并保持关联数据一致 |
| 17 | P-19 查看本人课程的学生名单 | P-18 | 查看本人课程的学生名单 |
| 18 | P-20 查看全部学生的当期选课情况 | P-19 | 查看全部学生的当期选课情况 |
| 19 | P-09 完成双角色学期周课表 | P-14、P-15、P-17、P-18 | 完成双角色学期周课表 |
| 20 | P-10 完成恢复、容量与部署验收 | P-09、P-20 | 完成恢复、容量与部署验收 |

### 1.3 可用交付节点

- 设计基线：P-01～P-03，业务已确认，技术和交互具备实现依据。
- 账号与课程准备：P-04、P-11、P-12、P-05～07、P-13，管理员可维护资料，教师可录入发布，学生可浏览。
- 选课及变更：P-08、P-14～18，四门上限、冲突校验、课程修改删除、退换课及关联删账号均可验证。
- 查询与验收：P-19、P-20、P-09、P-10，名单、全部学生概览、课表和交付验收完成。

### 1.4 验证命令与证据约定

当前仓库为文档阶段，没有 backend 或 frontend 工程。以下是本计划约定的**后续交付入口**，不是声称当前可以运行的命令。P-02 应验证工具适配并固定接口，P-04 起随相关功能交付真实脚本；如工具不支持约定参数，必须在业务实现前同步修订命令契约和各任务检查项。

| 命令 | 责任与用途 |
| --- | --- |
| `mvn -f backend/pom.xml verify` | 后端单元与真实 MySQL 集成检查；P-04 建立入口，各任务增加自己必要的证据 |
| `pnpm --dir frontend typecheck` | 前端类型检查，P-04 建立 |
| `pnpm --dir frontend lint` | 前端静态检查，P-04 建立 |
| `pnpm --dir frontend test -- --run` | 非交互前端逻辑验证，P-04 起按必要行为交付 |
| `pnpm --dir frontend build` | 构建检查，P-04 建立，交付验收运行 |
| `pnpm --dir frontend test:e2e` | 自动准备或连接规定的测试服务，执行真实浏览器流程；P-04 建立 |
| `pnpm --dir frontend test:performance` | 以 P-02 固定的环境、负载及工具执行性能验证，P-10 交付 |

所有命令默认从仓库根目录执行。后端检查必须使用隔离的目标 MySQL 测试实例，不能访问真实业务库；测试时钟、数据初始化、清理、服务启动和配置变量的契约统一写入 design.md；P-02 实际验证步骤和结果记录在 docs/verification/P-02.md。不得用内存数据库替代事务与并发证明，也不建立空测试使脚本虚假通过。

每项任务记录 docs/verification/P-编号.md，内容包含需求和设计版本、环境、夹具、实际命令、观察结果及必要截图或日志。未执行项写明“未执行”。根目录 design.md 和 interaction-design.md 是已交付的技术与交互契约，不再维护重复的 docs/design 三份技术拆分稿；docs/verification 路径仍为后续实际证据产物，不代表文件已经存在。

设计和任务中的检查以解决具体风险为准。文档任务采用一致性和人工走查，不生成形式化业务测试；实现任务交付自身必要验证，跨任务验收可累积证据，不重复执行无关检查。

## 2. 任务说明

### P-01 固定首版业务规则和边界

**状态：已完成，本轮文档交付**

#### 要构建什么？

将用户已采纳的全部规则固化为正式需求，消除业务默认项的待确认状态。

#### 为什么？

后续设计与实现需要一致且稳定的业务依据。

#### 完成条件

- requirement.md v1.0.6 已包含三角色、学期、课程管理、退换课及教师学生查询。
- 全部默认规则已采纳，业务待定项已关闭；技术验证未被误标为完成。
- 既有需求、不变量、验收编号保留，配套计划和追踪关系同步。

#### 如何检查

- 对照用户确认与 requirement.md 第 13～14 节核对范围和定稿状态。
- `rg -n '^REQ-|^- INV-|^\| AC-' requirement.md`，核对 53 项需求、16 条不变量、38 项验收。

#### 执行备注

- 依赖：无。
- 来源：requirement.md v1.0.6；本任务完成依据是本次用户采纳及两份文档更新。
- 不再重新询问已经采纳的规则；状态不表示设计或开发已经完成。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

工程代码、技术版本验证和真实部署。

### P-02 固定可实施的技术及验证契约

**状态：进行中；design.md 已交付，环境安装、精确依赖与隔离兼容验证未完成**

#### 要构建什么？

形成全部功能共同遵守的技术设计，验证指定版本能组成可运行环境，并明确数据、接口、权限、重试和测试方案。

#### 为什么？

并发选课、换课、课程管理和删账号会修改相关数据，必须使用同一套一致性机制。

#### 完成条件

- 保留用户指定技术栈，记录官方兼容性依据和必要的隔离验证，锁定前端、后端、MySQL 及运行时精确版本。
- 用户已确认 Java 21 LTS：P-02 环境准备安装并记录发行版、精确补丁及校验摘要，配置 JAVA_HOME 和 PATH；IDE、Maven、测试及部署使用同一 JDK 21 基线，编译 release=21。本轮只记录选择，环境安装在逐项确认完成后统一执行。
- 接口契约覆盖三角色身份、师生账号、学期、课程、两类名单、选退换课及操作结果查询；明确错误码、分页、状态、版本和授权范围。
- 数据方案固定标识、唯一约束、逻辑删除、历史归属、有效选课代次、学期生成及管理员初始化；空库路线与实际接入旧库时的条件路线分开。
- 用顺序与交错时序证明全部写入遵守不变量，固定事务和锁顺序、资源版本、重试上限及死锁处理；结束边界统一采用用户已确认的锁内 decisionTime：读取相关状态并完成前置校验后、领域写入前取数据库时间，持锁至提交或回滚。结束前通过判定可在结束后提交；排队或回滚重试必须重新判定。
- 固定操作标识的作用域、保留时长、过期行为、同键不同内容拒绝和重放流程；未登录注册的请求去重不得泄露他人注册结果。
- 固定密码散列、会话存储和失效、CSRF、管理员初始化与密码维护、限流阈值/维度、日志脱敏及运维恢复方式。
- 确定统一检查命令、真实 MySQL 隔离方式、可控时钟和故障注入方法、性能测试硬件/负载/数据、浏览器及截图证据规则。
- 用户已确认 MySQL 8.4 LTS：准备独立开发实例及独立测试实例，固定同一精确补丁和发行包或镜像摘要。使用独立端口、数据目录或卷、专用凭据和明确连接配置；保留本机 8.0.27 实例及数据，不原地升级、不自动迁移旧库。容器运行环境已确认为 Docker Desktop + WSL 2，使用 Linux 容器；安装在全部选择完成后统一执行。开发 MySQL 容器使用专用持久卷，Testcontainers 为集成测试创建独立临时容器，不共享开发卷。

#### 如何检查

- 产物：根目录 design.md，集中承载技术、数据、接口及验证方案；实际环境和兼容验证证据保存至 docs/verification/P-02.md。逐条建立 INV-01～16、AC-01～38 到设计机制的对应关系。
- `rg -n 'INV-|AC-|REQ-' design.md interaction-design.md`，辅助核对覆盖，再人工审查时序与接口，不以文本命中代替设计证明。
- 演算并发第五门、两门冲突、双目标换课、选课与编辑/下架/删除、删除学生与选课、学期结束、旧请求在重新选入后重放。
- 兼容验证记录实际命令和输出；未执行的验证不填写通过。
- Docker 准备前核对 Windows 版本/构建及受支持状态、WSL 版本、虚拟化和内存条件；核验通过后安装并记录 Docker Desktop、WSL 和 Compose 精确版本。检查 docker version 的 Client/Server、docker info 的 Linux 容器模式、docker compose version；隔离 Testcontainers 验证须实际启动固定 MySQL 8.4 镜像、建立连接、执行检查并完成资源清理。启动失败或没有容器运行环境时不得静默跳过测试。
- MySQL 准备完成后连接目标实例执行只读版本、端口、字符集、排序规则和隔离级别检查；隔离验证唯一键、CHECK、外键和事务回滚，记录实际结果。开发连接和测试连接必须分别核对，不能仅凭本机 mysql 客户端版本认定服务器达标；测试初始化和清理只作用于本次创建的测试资源。
- 安装后在新终端核对 java -version、javac -version 和 mvn --version 均使用目标 JDK 21，Maven 保持 3.9.16；隔离验证程序按 release=21 编译并运行成功，才关闭 Java 环境项。现有 Maven 3.9.16 已核实，不需重复升级；当前 Maven 仍使用 Java 17.0.1，不可把 Maven 达标视为 Java 环境已完成。

#### 执行备注

- 依赖：P-01 固定首版业务规则和边界。
- 来源：requirement.md v1.0.6，重点 REQ-02、05、08、31～36、38～45、48～50、52～53。
- 本任务是共享契约唯一负责人；设计必须消除会改变行为、数据、接口、安全和证明的技术选择，再放行实现。
- 不把兼容性未知转成默认改用其他框架；确实不满足指定版本时先记录证据并走需求变更。
- 技术设计已保存为 design.md；文件存在不等于环境与兼容验证已经通过，P-02 仍需补齐实际证据。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

业务功能实现、公网部署、重新决定已采纳的产品规则。

### P-03 固定表单、列表与周课表交互

**状态：进行中；interaction-design.md 已交付，静态接口与流程走查完成；视觉样张、灰度及色觉差异模拟未执行**

#### 要构建什么？

定义三个角色的页面、导航和操作状态，让管理员管账号、教师管课程和查学生、学生选退换课的完整流程都能走通。

#### 为什么？

用户需要清楚识别学期、课程状态、操作影响和失败结果，避免误操作。

#### 完成条件

- 给出三角色导航、注册登录、管理员账号表单和查询、教师课程与两类学生查询、学生目录/已选列表的页面方案。
- 明确学期选择、历史只读、未上架/已上架/已下架/已删除的可见范围和操作入口。
- 账号和课程删除、退课、换课有对象与影响确认；换课结果待确认时可查询，不诱导先退后选。
- 规定加载、空、错误、冲突、版本过期、会话失效和超时状态，以及表单值保留规则。
- 周课表符合五天、半小时格、午休、固定 12 色可复用调色板、稳定颜色与边框、长名称、教师重叠、学期日期和下架标记要求；走查超过 12 门、同色重叠、灰度及色觉差异模拟，编号和名称须可读。
- 桌面与窄屏布局、键盘访问及不单靠颜色区分课程均可走查；与技术接口逐项一致。

#### 如何检查

- 产物：根目录 interaction-design.md，含流程、低保真示意与状态表；design.md 是唯一技术契约。已完成的文档走查见交互稿第 10 节，可视化样张与后续证据另记 docs/verification/P-03.md。
- 人工演示账号建改删、教师录入上架、四门情况下换课、退课后重选、全部学生中未选课及同名学生、历史学期查询。
- `rg -n '学期|换课|删除|重叠|超时|键盘|390|1366' interaction-design.md`，再逐图核对行为。

#### 执行备注

- 依赖：P-01 固定首版业务规则和边界。
- 来源：requirement.md v1.0.6，REQ-04、09～16、22～30、37～49、51；AC-15～16、21、23～24、27～37。
- 与 P-02 汇合时核对错误和请求状态，二者一致后才进入业务实现。
- 只设计既定范围，不增加管理员代选、真实日期月历或课程拖拽。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

业务代码、额外产品功能和品牌设计项目。

### P-04 完成三类账号注册登录及隔离

**状态：未开始**

#### 要构建什么？

让师生注册获得账号并登录对应系统，管理员用预置账号登录管理系统，所有角色可安全退出。交付能运行的首个完整业务流程。

#### 为什么？

所有课程和账号操作都依赖有效身份及明确的权限边界。

#### 完成条件

- 工程使用已经验证的版本和目标 MySQL，身份相关初始化、启动和测试入口可以实际运行。
- 师生姓名、编号、邮箱和双密码校验、随机唯一账号及注册成功展示完整实现。
- 首次初始化 admin/admin，重启不重复创建或覆盖已有密码，公共注册不能生成管理员。
- 有效会话可刷新恢复；退出和超时后无法读写；会话模型支持后续密码重置和账号删除时使全部会话失效。
- 角色路由与接口均隔离，登录错误、注册碰撞、频率限制、密码存储及日志符合共享契约。
- 关闭 AC-01、AC-02、AC-03、AC-04、AC-22，提供 AC-17、AC-20、AC-21 的身份部分证据。

#### 如何检查

- `mvn -f backend/pom.xml verify`
- `pnpm --dir frontend typecheck`
- `pnpm --dir frontend test -- --run`
- `pnpm --dir frontend test:e2e`
- 浏览器完成三角色登录，直接请求越权接口；模拟随机账号碰撞、会话过期、重复初始化及注册响应丢失。

#### 执行备注

- 依赖：P-02 固定可实施的技术及验证契约；P-03 固定表单、列表与周课表交互。
- 来源：requirement.md v1.0.6，REQ-01～08、35、38、50；INV-01、02、10。
- 实现 P-02 定义的共享认证、去重和初始化基础，其他模块后续扩展，不建立空业务接口。
- 学生与教师初始密码不继承 admin 的五字符例外；首版不额外增加首次改密。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

管理员师生资料管理、学期及课程功能。

### P-11 按固定日期生成学期并支持切换

**状态：未开始**

#### 要构建什么？

生成当前和下一学年的春秋学期，让各角色看到准确起止日期，并能选择未来或历史学期。

#### 为什么？

课程、课表和选课限制必须有明确的学期归属。

#### 完成条件

- 每年 9 月 1 日切换学年，生成当前及下一学年的四个学期，历史记录保留，重复生成不重复写入。
- 固定开课日和 84 天半开区间正确，周末开课不顺延，使用学校时区而非浏览器时区。
- 默认选择进行中学期，否则选择最近未开始学期；空学期和历史学期均可选择并清楚展示状态。
- 提供共享的学期查询与写入时间边界判断，后续每个写入任务实际接入该规则。
- 关闭 AC-25，提供 AC-26、AC-36 的日期与上下文基础证据。

#### 如何检查

- `mvn -f backend/pom.xml verify`
- `pnpm --dir frontend test:e2e`
- 使用可控时钟验证 9 月 1 日、3 月 1 日、84 天结束边界、跨年及开课日为周末；切换客户端时区不改变日期。

#### 执行备注

- 依赖：P-04 完成三类账号注册登录及隔离。
- 来源：requirement.md v1.0.6，REQ-41～42、51；INV-11～12。
- 没有课程写入时只能证明学期能力；不得提前把所有业务的结束只读验收标成通过。
- 不引入独立学期管理角色或任意起止日期编辑。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

课程自动复制、单双周、节假日调课。

### P-12 让管理员新增查询并编辑师生账号

**状态：未开始**

#### 要构建什么？

提供管理员的师生账号列表、搜索、详情、新增、资料编辑和密码重置，让账号资料可以维护。

#### 为什么？

管理员需要协助建立账号、纠正资料以及处理遗失登录信息。

#### 完成条件

- 按角色筛选、姓名/账号/编号查询和稳定分页，详情只返回允许的账号字段。
- 新增师生复用注册规则和随机账号生成；自助注册与管理员创建并发时遵守相同唯一约束。
- 姓名、编号、邮箱可编辑，账号和角色不可编辑；全部校验通过才保存，重置密码须两次一致。
- 密码留空表示不修改；重置后旧密码和全部旧会话不可继续使用，不返回原密码或摘要。
- 编辑版本冲突、同请求重放、管理员保护和跨角色拒绝有直接接口证明。
- 关闭 AC-23，提供 AC-17、AC-37 的账号新增编辑部分证据。

#### 如何检查

- `mvn -f backend/pom.xml verify`
- `pnpm --dir frontend typecheck`
- `pnpm --dir frontend test:e2e`
- 创建两类账号并登录；验证重复邮箱/编号、重名、空姓名、伪造角色、旧版本提交、重置后的旧会话以及多页资料查询。

#### 执行备注

- 依赖：P-04 完成三类账号注册登录及隔离。
- 来源：requirement.md v1.0.6，REQ-39、50、52；INV-01、02、10。
- 删除账号及关联课程处理由 P-18 交付；此阶段不放置无效删除按钮。
- 共享资料编辑不得改动固定 admin 账号。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

账号删除、师生自助修改密码、管理员代选课。

### P-05 完成合法课程的可靠创建

**状态：未开始**

#### 要构建什么？

教师选择学期并填写课程内容和一至两次授课安排，保存后获得未上架课程及可再次查询的结果。

#### 为什么？

后续选课和课表必须基于合法、完整、可持久化的课程数据。

#### 完成条件

- 学期、名称、描述和授课安排完整校验，课程绑定本人教师，编号唯一，初始未上架。
- 一至两次不同日安排、半小时对齐、允许时段和全部合法边界成立。
- 课程与安排整体提交；重复同一操作只创建一次，超时可查询实际结果。
- 非法角色、他人归属、已结束学期、参数与学期不符均被服务端拒绝。
- 关闭 AC-06、AC-07，提供 AC-05 的创建部分及 AC-16、AC-18、AC-36～38 的相关证据。

#### 如何检查

- `mvn -f backend/pom.xml verify`
- `pnpm --dir frontend test -- --run`
- `pnpm --dir frontend test:e2e`
- 提交午休、周末、三次安排、同日两次和时间边界；在第二条安排保存时注入故障，检查无半成品；重试保存并刷新查询。

#### 执行备注

- 依赖：P-11 按固定日期生成学期并支持切换。
- 来源：requirement.md v1.0.6，REQ-09～12、41～42、50；INV-03、04、08、11～12。
- 仅实现保存结果确认所需读取；完整列表由 P-06 交付，上架由 P-13 交付。
- AC-05 包含上架后的学生可见性，创建完成不能独自关闭该项。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

课程编辑删除上下架、选课、完整课表。

### P-06 完成教师本人课程查询

**状态：未开始**

#### 要构建什么？

教师按学期查看自己创建的课程列表和详情，确认课程内容、上架状态及选课人数。

#### 为什么？

课程较多时仍需完整查询，且管理数据只能由课程所有者访问。

#### 完成条件

- 列表包括编号、名称、学期、状态、有效选课人数、全部安排和详情，稳定排序及分页符合契约。
- 未上架、下架和历史课程可在各自学期查看，已删除课程按规则排除。
- 教师只能查询本人管理数据，客户端修改账号或课程编号不能扩大权限。
- 空、加载失败和重试状态完整，刷新后数据仍可获取。
- 关闭 AC-08，提供 AC-19 的教师列表证据；人数用隔离夹具验证，真实选退换联动在后续任务补证。

#### 如何检查

- `mvn -f backend/pom.xml verify`
- `pnpm --dir frontend test:e2e`
- 使用两名教师、两个学期及超过一页课程，核对列表完整性、状态、详情和直接越权请求。

#### 执行备注

- 依赖：P-05 完成合法课程的可靠创建。
- 来源：requirement.md v1.0.6，REQ-13～14、32、42、51；INV-02、09。
- 课程名单由 P-19、课表由 P-09 接入，未交付前不显示不可使用的入口。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

课程管理写入、学生名单、课表绘制。

### P-13 上架和下架本人课程

**状态：未开始**

#### 要构建什么？

教师可以发布课程供学生选择，也可以停止新的选择，且下架保留已有选课。

#### 为什么？

课程保存与对学生开放需要分开，停止招生不应清空学生课表。

#### 完成条件

- 未上架→已上架、已上架→已下架、已下架→已上架转换正确，界面与接口状态一致。
- 只有本人、未删除且学期未结束的合法课程可转换状态；重复操作按共享契约返回。
- 下架不改有效选课、人数和安排；重新上架不换课程身份。
- 状态版本和操作重放处理正确，与后续选课共用 P-02 的状态互斥机制。
- 提供 AC-28、AC-33、AC-36～37 的上下架部分证据；学生目录和选课落地后完成相应集成验证。

#### 如何检查

- `mvn -f backend/pom.xml verify`
- `pnpm --dir frontend test:e2e`
- 演示三个状态转换、历史学期拒绝、重复点击和旧版本；用真实 MySQL 夹具验证下架前后已有关系不变。

#### 执行备注

- 依赖：P-06 完成教师本人课程查询。
- 来源：requirement.md v1.0.6，REQ-44、50；INV-12～14。
- P-07 在真实学生目录中证明已上架课程可见后关闭 AC-05；AC-28 在 P-17 的换入场景与 P-09 的课表场景完成后，由 P-09 汇总关闭。
- 不要提前实现选课接口以满足本阶段尚未具备的场景。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

课程删除、编辑、强制退课。

### P-07 完成学生可选课程浏览

**状态：未开始**

#### 要构建什么？

学生按学期浏览已上架课程及完整详情，识别课程内容与全部上课时间。

#### 为什么？

学生需要在明确学期和可选状态下比较课程，不能误选未发布内容。

#### 完成条件

- 目录仅包含所选学期已上架且未删除课程，显示编号、名称、完整时间和描述入口。
- 按编号数字部分排序并分页，课程重名不混淆，空和读取失败有可恢复状态。
- 预置已选数据时正确显示已选状态；下架课程退出目录但不改变已有关系。
- 普通目录和详情不泄露教师注册资料，学生之外角色不能进入该功能。
- 结合 P-05 和 P-13 关闭 AC-05，提供 AC-09、AC-19、AC-28 的目录证据。

#### 如何检查

- `mvn -f backend/pom.xml verify`
- `pnpm --dir frontend test:e2e`
- 使用跨学期、同名、多页、不同上架状态课程；检查实际响应字段，不能只在页面隐藏私有信息。

#### 执行备注

- 依赖：P-13 上架和下架本人课程。
- 来源：requirement.md v1.0.6，REQ-15～16、32、42、44；INV-02、09、13。
- 暂不展示无效选课按钮；P-08 接入真实选课后关闭 AC-09。
- 已选下架课程详情通过本人已选授权入口提供，不能放宽公开课程目录。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

选课写入、退换课、容量与候补。

### P-08 完成无冲突且不超额的选课

**状态：未开始**

#### 要构建什么？

学生可选入课程，查看本人当期已选列表及四门计数，超额或冲突时得到明确原因。

#### 为什么？

多页面同时提交也必须遵守每学期四门和无冲突规则。

#### 完成条件

- 选择课程包含全部安排，跨学期分别计数；列表显示保留选课的下架课程和完整详情。
- 全部授课安排参与冲突判断；相邻区间和不同日相同时间允许，候选第二次安排重叠也拒绝。
- 已选重试只返回已有关系；新选要求账号、学期、课程上架与删除状态合法。
- 真实 MySQL 并发提交不超四门、不留冲突关系；与课程上下架形成正确顺序结果。
- 成功更新状态、计数和已选列表；失败不清空，超时可以确认结果；学生只能为本人选择。
- 关闭 AC-09～13，提供 AC-14、AC-16、AC-19、AC-26、AC-28、AC-33、AC-36～37 的选课部分证据；AC-14 的课表一致性由 P-09 最终关闭。

#### 如何检查

- `mvn -f backend/pom.xml verify`
- `pnpm --dir frontend test -- --run`
- `pnpm --dir frontend test:e2e`
- 同步释放并发第五门、互相冲突课程、同一课程重复请求；验证跨学期各选四门、多标签页、响应丢失及下架竞态。

#### 执行备注

- 依赖：P-07 完成学生可选课程浏览。
- 来源：requirement.md v1.0.6，REQ-17～24、44、50；INV-05～08、11～14。
- 页面按钮禁用不能替代事务保障，必须使用目标 MySQL 证明一致性。
- 退课、换课和课表入口由后续任务逐项启用，不以空入口宣称完成。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

退换课、课表绘制、课程修改和账号删除。

### P-14 编辑课程内容并保护已有选课

**状态：未开始**

#### 要构建什么？

教师可以纠正课程名称、描述和允许修改的授课安排，已经有人选课时保留原上课时间。

#### 为什么？

修改课程不能让已选学生突然产生时间冲突或丢失课程。

#### 完成条件

- 本人未结束学期课程的名称和描述可修改，课程编号、归属和学期不变，上架状态不变。
- 无人有效选课时可修改星期、起止时间和安排条数，复用全部创建校验。
- 有人有效选课时拒绝任何授课安排变化；失败保留原字段和选课，不通过强制退课完成修改。
- 时间编辑与选课互斥：编辑先完成则按新时间选课，选课先完成则时间编辑拒绝。
- 旧版本新操作、重复请求、他人课程及历史课程处理正确；后续查询显示更新内容。
- 关闭 AC-27，提供 AC-33、AC-36～37 的编辑证据。

#### 如何检查

- `mvn -f backend/pom.xml verify`
- `pnpm --dir frontend test:e2e`
- 并发编辑时间与选课，检查事务结果；尝试非法安排、改学期、改归属、编辑已结束课程和两次旧版本提交。

#### 执行备注

- 依赖：P-08 完成无冲突且不超额的选课。
- 来源：requirement.md v1.0.6，REQ-43、50；INV-03、04、08、12、14。
- 不因增加编辑功能放宽创建约束；姓名修改与课程时间修改的权限条件不同。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

删除、课程复制、强制取消学生选课。

### P-15 删除无有效选课的本人课程

**状态：未开始**

#### 要构建什么？

教师确认课程信息后可以删除符合条件的课程，日常列表移除该课程并保留历史。

#### 为什么？

误建课程可以清理，但删除不能破坏学生已经确认的选择。

#### 完成条件

- 删除确认展示编号、名称、学期和影响，只有本人、未结束、零有效选课课程允许删除。
- 逻辑删除后从教师列表、学生目录移除，保存编号、安排与操作历史且不能恢复或复用编号。
- 有有效选课、他人课程和已结束课程删除均拒绝且不改变数据。
- 重复同操作返回删除结果，旧版本新请求拒绝，删除与选课不能同时成功留下有效关系。
- 完成 AC-29 的删除行为及 AC-33、AC-36～37 的删除证据；课表移除在 P-09 补证后最终关闭 AC-29。

#### 如何检查

- `mvn -f backend/pom.xml verify`
- `pnpm --dir frontend test:e2e`
- 验证确认取消、允许/拒绝删除、直接越权、并发删除与新选，查询历史及编号保留。

#### 执行备注

- 依赖：P-08 完成无冲突且不超额的选课。
- 来源：requirement.md v1.0.6，REQ-45、50、52；INV-08、12～14。
- 共享删除标记和幂等结果遵循 P-02，不物理级联清空课程或选课。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

账号删除、恢复课程、批量强制退课。

### P-16 退课并允许按规则重新选入

**状态：未开始**

#### 要构建什么？

学生可以确认退掉未结束学期的已选课程，释放名额，也可在课程仍可选时重新加入。

#### 为什么？

学生需要调整安排，同时避免网络重试误撤销后来重新选择的课程。

#### 完成条件

- 可退掉本人有效选课，包括下架课程；历史学期拒绝，确认展示正确课程和学期。
- 成功使有效关系失效并更新计数和已选列表，保留退课操作历史。
- 同一次退课重试不再改变数据；重新选入生成新的选课代次，旧退课不能取消新选择。
- 越权、旧版本、超时和并发选退按既定契约处理，不取消其他有效选择。
- 完成 AC-30 的退课与重选行为及 AC-33、AC-36～37 的相关证据；AC-30 在全部视图落地后由 P-10 汇总关闭。

#### 如何检查

- `mvn -f backend/pom.xml verify`
- `pnpm --dir frontend test:e2e`
- 退掉下架课、重新选入、回放旧退课、模拟响应丢失并查询结果；直接尝试操作其他学生关系。

#### 执行备注

- 依赖：P-08 完成无冲突且不超额的选课。
- 来源：requirement.md v1.0.6，REQ-48、50、52；INV-05～09、12。
- 必须绑定来源关系版本或代次，不能只凭学生和课程的组合再次执行旧退课。
- 关闭验收时记录该任务直接覆盖的结果，尚未交付的视图投影列入后续证据清单。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

换课、管理员代退、历史学期修改。

### P-17 原子换课并在失败时保留来源

**状态：未开始**

#### 要构建什么？

学生可把一门已选课程替换成同学期另一门可选课程，失败时保留原课程。

#### 为什么？

先退后选可能让学生失去原选择，换课必须作为一次完整操作完成。

#### 完成条件

- 来源有效且属于本人，目标在同一未结束学期、已上架未删除；已下架来源仍可换出。
- 以移除来源再加入目标后的集合检查数量和冲突；四门时可一换一，目标仅与来源重叠不导致拒绝。
- 目标已是其他已选课时拒绝；来源目标相同无修改；跨学期和其他非法目标拒绝。
- 换入换出整体提交，任何失败保留原集合，更新两门课有效关系和人数。
- 同来源并发换向两目标仅一次有效替换；操作重放不撤销重新选入的新代次；超时可确认结果。
- 关闭 AC-26、AC-32，提供 AC-28、AC-31、AC-33、AC-36～37 的换课证据；AC-28 在 P-09、AC-31 在 P-10 完成全部视图后关闭。

#### 如何检查

- `mvn -f backend/pom.xml verify`
- `pnpm --dir frontend test -- --run`
- `pnpm --dir frontend test:e2e`
- 在四门、来源下架、目标冲突/已选/删除/跨学期下验证；注入换出后的事务故障；并发双目标换课与目标下架。

#### 执行备注

- 依赖：P-16 退课并允许按规则重新选入。
- 来源：requirement.md v1.0.6，REQ-49～50；INV-05～08、11～14。
- 不得把前端顺序调用退课与选课接口当作换课实现。
- 关闭 AC-26 时汇总 P-08 的跨学期选课证据；上下架和换课的视图投影继续由后续任务验证。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

跨学期换课、多门批量替换、容量抢位。

### P-18 删除师生账号并保持关联数据一致

**状态：未开始**

#### 要构建什么？

管理员可删除符合条件的师生账号，立即阻止登录，并正确处理当前选课与历史课程。

#### 为什么？

账号删除不能留下可继续使用的会话、遗漏的当前选课或丢失的历史归属。

#### 完成条件

- 删除界面显示目标、资料版本及关联影响；不能删除或改变 admin。
- 删除学生与取消其全部未结束学期有效选课原子提交；历史最终选课保留，后续历史读取可显示账号已删除。
- 教师有未结束学期未删除课程时拒绝；只有历史课程时可逻辑删除且保留教师归属。
- 全部旧会话立即失效，唯一账号/编号/邮箱不释放；重复同删除操作不再次取消关系。
- 与选课、换课、改课程时间、删课程、注册及密码重置的并发遵守统一事务和版本规则。
- 关闭 AC-33，完成 AC-24 的账号删除与历史保留部分及 AC-17、AC-36～37 的相关证据；历史名单由 P-19 补证后关闭 AC-24。

#### 如何检查

- `mvn -f backend/pom.xml verify`
- `pnpm --dir frontend test:e2e`
- 构造学生两个未结束学期及一个历史学期的关系；删除后检查全部当前关系、历史、会话和唯一值。
- 覆盖教师删除拒绝及允许场景；并发删除学生与选换课，确认无半删除和删除后新写入。

#### 执行备注

- 依赖：P-12 让管理员新增查询并编辑师生账号；P-14 编辑课程内容并保护已有选课；P-15 删除无有效选课的本人课程；P-17 原子换课并在失败时保留来源。
- 来源：requirement.md v1.0.6，REQ-40、50、52；INV-01、08、12、15。
- 不能借删账号越过课程有学生时禁止删除的约束。
- AC-33 的最终责任在本任务，因为此前的全部相关写入在此已经具备；不得只测账号自身接口。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

物理擦除历史、账号恢复、管理员直接代选退课。

### P-19 查看本人课程的学生名单

**状态：未开始**

#### 要构建什么？

教师可以从自己的课程进入学生名单，查看姓名、账号、选课时间和人数，并区分历史结果。

#### 为什么？

教师需要掌握每门课的选课人员，同时维护课程管理数据的归属边界。

#### 完成条件

- 入口显示正确学期、课程编号、名称和有效人数，名单按学生账号升序分页。
- 仅显示本人课程的有效选课；下架保留名单，退课、换出、未结束学期账号删除后名单同步变化。
- 历史学期名单保留最终选择，被删除学生显示标记；空名单及多页名单可完整读取。
- 访问其他教师课程专用名单拒绝，即使教师具有全体学生只读总览权限。
- 响应仅包含授权字段，不含邮箱、注册编号、密码摘要或会话；关闭 AC-24、AC-34，汇总账号删除与历史名单证据。

#### 如何检查

- `mvn -f backend/pom.xml verify`
- `pnpm --dir frontend test:e2e`
- 通过真实选退换和删学生操作刷新名单，核对人数；验证历史被删账号、空状态、多页以及直接篡改课程编号。

#### 执行备注

- 依赖：P-18 删除师生账号并保持关联数据一致。
- 来源：requirement.md v1.0.6，REQ-46、32、52；INV-02、09、10、16。
- 实现名单投影时补充 AC-24、30～31 的可见结果证据，不重复实现写入规则。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

其他教师专用课程名单、账号编辑、批量消息或导出。

### P-20 查看全部学生的当期选课情况

**状态：未开始**

#### 要构建什么？

教师可以按学期查看全部未删除学生的姓名、选课数量及完整课程清单，包括未选过本人课程的学生。

#### 为什么？

教师需要全体学生的选课概览，不能把查询范围误缩小成自己的授课名单。

#### 完成条件

- 包含全部未删除学生及未选课学生，同名以学生登录账号区分。
- 按姓名/账号查询并筛选全部、已选、未选；分页稳定，0/4 显示明确。
- 每个学生显示完整当期有效课单，包含其他教师和已下架但保留选择的课程，不静默截断。
- 切换学期分别计数，历史展示当期最终结果，删除账号不进入日常总览。
- 响应只包含授权资料；页面和接口均只读，不允许借查询代选、退课或获取账号私有资料。
- 关闭 AC-35，补齐学生总览相关 AC-19、AC-24、AC-30～31 的联动证据。

#### 如何检查

- `mvn -f backend/pom.xml verify`
- `pnpm --dir frontend test:e2e`
- 测试未选、仅选其他教师课程、重名、下架课、两个学期、多页学生；执行选退换及账号删除后重新查询。
- 检查嵌套响应字段和篡改写请求，证明不存在多余权限。

#### 执行备注

- 依赖：P-19 查看本人课程的学生名单。
- 来源：requirement.md v1.0.6，REQ-47、32、51～52；INV-02、09、10、16。
- 不能复用教师本人课程过滤条件；不用每名学生单独一次请求的页面循环掩盖整体性能问题，查询方案遵循 P-02。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

跨学期混合计数、学生互查、账号管理及写操作。

### P-09 完成双角色学期周课表

**状态：未开始**

#### 要构建什么？

教师和学生可以从自己的课程列表进入所选学期周课表，直观看到上课安排及课程状态。

#### 为什么？

课程列表与课表必须一致地反映上下架、退换课和删除后的结果。

#### 完成条件

- 教师显示本人未删除课程并标记状态，学生显示本人有效选择且保留下架课；整学期数据不受列表分页影响。
- 五个工作日、08:00～18:00 二十个半小时格、午休和课程起止位置正确。
- 同课程两次安排使用固定 12 色表中的同色和同一边框，列表/详情/课表、刷新、分页及选退换课后样式稳定；颜色与边框组合允许复用，编号与名称可读；教师同色重叠均可见，学生异常数据不被隐藏。
- 显示所选学期和日期、仅在学期内重复的说明；切换学期不混入旧数据，历史只读。
- 长名称、鼠标/键盘/触摸访问、桌面窄屏、空/错误/会话失效状态符合设计。
- 关闭 AC-14、AC-15、AC-28、AC-29，汇总其前序业务证据并补齐课表；提供 AC-19、21、25、30～31、36 的课表投影证据。

#### 如何检查

- `mvn -f backend/pom.xml verify`
- `pnpm --dir frontend typecheck`
- `pnpm --dir frontend test -- --run`
- `pnpm --dir frontend test:e2e`
- 在 1366×768、390×844 检查超过 12 门教师课程、同色重叠、长名称、两次安排、下架保留、退换课与删课/账号后结果；跨列表/详情/课表及分页核对颜色和边框稳定，保存正常与灰度视图截图。

#### 执行备注

- 依赖：P-14 编辑课程内容并保护已有选课；P-15 删除无有效选课的本人课程；P-17 原子换课并在失败时保留来源；P-18 删除师生账号并保持关联数据一致。
- 来源：requirement.md v1.0.6，REQ-25～30、51；INV-09、11～13。
- 复用展示逻辑不意味着放宽数据权限；周模板不引入第十三周、客户端时区偏移或真实日期月视图。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

拖拽排课、打印导出、月视图、跨学期自动复制。

### P-10 完成恢复、容量与部署验收

**状态：未开始**

#### 要构建什么？

交付可复现运行说明和完整验收报告，证明系统在初始化、故障恢复和目标负载下符合正式需求。

#### 为什么？

业务演示通过还需要可重复的交付和跨功能验收证据。

#### 完成条件

- 逐条汇总 AC-01～38，复用前序证据并补齐所有页面投影及交叉行为，不能只用通过的单元测试代替业务验收。
- 证明数据库故障、恢复、重启、事务中断及就绪状态行为；管理员不会被重置；实际演练隔离环境备份恢复。
- 按 P-02 固定条件完成两个学期、目标用户课程规模和十分钟混合负载，报告 P95、非预期错误率、并发一致性和资源数据。
- 记录 Chrome、Edge 实际版本、两种视口、键盘可用性、首次可操作时间、限流及敏感信息检查。
- 通过可控时钟逐项验证全部课程管理及选退换接口的学期结束边界：结束前判定后暂停跨界再写入提交仍允许；排队后及回滚重试时判定已到边界则拒绝；等于边界拒绝；失败回滚，成功回执重放不再写入。验证所有写入的旧版本、去重和重放契约。
- 从干净环境按说明初始化并完成三角色完整流程；空库提供初始化证据，实际接入旧数据时先按显式映射验证，未适用的迁移说明原因。
- 最终关闭 AC-16～21、AC-30～31、AC-36～38，核对所有其余验收的最终证据和已解决缺陷；未通过项不得标为完成。

#### 如何检查

- `mvn -f backend/pom.xml verify`
- `pnpm --dir frontend lint`
- `pnpm --dir frontend typecheck`
- `pnpm --dir frontend test -- --run`
- `pnpm --dir frontend build`
- `pnpm --dir frontend test:e2e`
- `pnpm --dir frontend test:performance`
- 在隔离数据环境执行故障和恢复，重建环境后按照 docs/runbook.md 完成管理员建号、教师发布、学生选退换、教师查询及课表流程。

#### 执行备注

- 依赖：P-09 完成双角色学期周课表；P-20 查看全部学生的当期选课情况。
- 来源：requirement.md v1.0.6，REQ-31～37、50、52～53及全部 AC。
- 产物：docs/runbook.md、docs/verification/acceptance-report.md、性能与恢复证据；不得包含真实密钥。
- 本任务仅补交付和跨功能证明；前序任务必须各自证明其行为。发现业务缺口回到对应任务修复，架构变更先更新设计。
- 所有实现任务额外遵守 P-02/P-03 定稿设计；本任务记录具体采用的版本。

#### 不包含

真实公网发布、真实学校身份集成、新业务功能。

## 3. 验收最终责任

每项 AC 只能在该项完整预期结果具备证据后关闭。前序任务完成页面、接口或数据部分后记录局部结果；本表的最终责任任务汇总全部证据。任务可以在其边界内完成，而跨视图的 AC 继续等待指定任务，不能两者混为一谈。

| 验收编号 | 场景主题 | 最终关闭任务 |
| --- | --- | --- |
| AC-01 | 分别注册教师和学生，填写有效姓名、编号、邮箱和相同的两次密码 | P-04 |
| AC-02 | 密码不同、空编号、非法邮箱、重复编号或邮箱；强制随机账号碰撞及并发注册 | P-04 |
| AC-03 | 正确与错误密码登录；教师访问学生接口，学生访问教师接口 | P-04 |
| AC-04 | 刷新页面、退出后重用会话、模拟会话超时 | P-04 |
| AC-05 | 教师在未结束学期保存含一条安排、含两条不同星期安排的课程 | P-07 |
| AC-06 | 无安排、三条安排、同日两条、周末、跨午休、超时段、非半小时、结束不晚于开始 | P-05 |
| AC-07 | 08:00～08:30、11:30～12:00、14:00～14:30、17:30～18:00；08:00～12:00、14:00～18:00 | P-05 |
| AC-08 | 两名教师各有课程，篡改用户或课程参数查看管理数据 | P-06 |
| AC-09 | 学生打开所选学期已上架课程目录，包含重名、未选和已选课程 | P-08 |
| AC-10 | 学生在同一学期依次选择四门无冲突课程，再提交第五门 | P-08 |
| AC-11 | 候选课程第二次安排与已有课重叠；同日相接；不同日同时间 | P-08 |
| AC-12 | 重复提交同一课程，含已选四门时重试；并发重复请求 | P-08 |
| AC-13 | 同学期已有三门时并发选两门不同课程；同学期并发选择两门互相冲突的课程 | P-08 |
| AC-14 | 两名学生各自选课，互相篡改查询或提交中的学生标识 | P-09 |
| AC-15 | 选择学期，查看包含两次安排的课程、长名称及教师重叠课程的周课表 | P-09 |
| AC-16 | 无课程、无选课、加载失败、课程保存失败、连续点击及写入请求超时 | P-10 |
| AC-17 | 在注册、描述等输入脚本或注入字符串，检查日志与未授权响应 | P-10 |
| AC-18 | 数据库中断、恢复、服务重启；课程写入中故障 | P-10 |
| AC-19 | 超过一页的课程数据、切换分页后查看课表 | P-10 |
| AC-20 | 固定兼容版本后按部署说明初始化环境并执行验证 | P-10 |
| AC-21 | 执行容量和性能测试、两种视口及两个浏览器验收、注册登录频率限制测试 | P-10 |
| AC-22 | 首次初始化后使用 admin/admin 登录；错误密码、伪造 admin 字符串、师生访问管理接口；重复初始化或重启 | P-04 |
| AC-23 | 管理员新增、查询、修改师生资料及重置密码；姓名为空、邮箱重复、非法角色、编辑 admin | P-12 |
| AC-24 | 删除有当前选课的学生、有未结束课程的教师、仅有历史课程的教师，随后重用会话并查询历史 | P-19 |
| AC-25 | 生成 2026—2027 学年两学期，重复生成；验证开课日、结束日、十二周及周末开课 | P-11 |
| AC-26 | 同一学生在秋、春学期各选四门且周时间相同；在其中一个学期尝试选择第五门；尝试跨学期换课 | P-17 |
| AC-27 | 编辑本人未结束学期的课程名称描述及时间；有学生时改时间；编辑他人、历史课程或学期归属 | P-14 |
| AC-28 | 课程保存、上架、选入、下架、再上架；下架后尝试新选和换入 | P-09 |
| AC-29 | 删除无有效选课课程、有有效选课课程、他人课程、已结束学期课程，重复删除 | P-09 |
| AC-30 | 退掉已选或已下架课程，重复同一次退课，重新选入，执行旧退课请求 | P-10 |
| AC-31 | 已选四门时一换一；目标仅与来源时间重叠；来源已下架 | P-10 |
| AC-32 | 换入与其他课程冲突、目标已选/下架/删除/不同学期、来源已失效，或换课中注入故障 | P-17 |
| AC-33 | 同一来源并发换向两个目标；选课与改时间/下架/删除并发；学生删除与选课并发 | P-18 |
| AC-34 | 教师查看本人上架/下架课程名单，无选课、退课、换出、删除学生及历史学生账号删除场景；请求其他教师课程专用名单 | P-19 |
| AC-35 | 全体学生中有重名、未选课、只选其他教师课程、下架课程选课、多页数据，切换学期 | P-20 |
| AC-36 | 锁内判定前后边界、判定后暂停跨界提交、排队跨界、回滚后重试、成功重放；历史/未来学期切换及进行中选入 | P-10 |
| AC-37 | 重复编辑、换课和账号管理请求；相同操作标识不同内容；过期资源版本；响应丢失后查询 | P-10 |
| AC-38 | 空库初始化；如存在旧数据，先检查并执行明确的迁移映射 | P-10 |

需求与实现覆盖详见 [requirement.md 第 12 节](requirement.md#12-追踪关系与验证策略)，逐项 REQ 编号均能找到负责任务；INV-01～16 通过该节 AC 映射与本表闭环。P-10 汇总全量报告，但不替代 P-04～20 各自必须交付的证明。

## 4. 设计任务的必须产物

| 事项 | 负责人 | 完成标准 |
| --- | --- | --- |
| 指定依赖可获取性和兼容组合 | P-02 | 官方依据、必要验证输出、精确版本；若失败则先修订技术约束 |
| 接口、权限、数据、标识、学期与状态 | P-02 | 无相互冲突的路径、字段、响应和数据归属 |
| 所有写入的事务、重放、版本与结束边界 | P-02 | 对并发与故障场景有唯一可验证结果和实现机制 |
| 会话安全、密码、管理员初始化及维护、限流 | P-02 | 明确算法/配置、失效规则、阈值和可重复测试方法 |
| 隔离测试、运行恢复、容量与迁移条件 | P-02 | 精确命令和环境；空库适用与旧库条件可区分 |
| 页面、错误状态、学期切换、名单与课表 | P-03 | 每条主流程和失败分支可走查，接口与交互一致 |

技术与交互文档已分别交付 design.md 和 interaction-design.md，接口及关键流程已完成静态走查；P-02 环境兼容验证、P-03 视觉样张核验仍未完成，业务实现继续按准入条件执行。六项逐项选择已记录，剩余为既有任务执行，不重新询问已采纳的规则。

## 5. 版本记录与变更纪律

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| 0.1 | 2026-09-04 | 原始十项设计与开发任务，依据较早业务范围 |
| 1.0 | 2026-09-04 | 同步正式 requirement.md v1.0；P-01 完成；保留旧编号主题，新增 P-11～20，补齐全部范围及验收最终责任 |
| 1.0.1 | 2026-09-04 | 同步用户选择 A：P-02 固定锁内判定，P-10 按 AC-36 验证跨边界提交、排队与回滚重试；不改变任务完成状态 |
| 1.0.2 | 2026-09-04 | 同步配色选项 A：P-02 定义固定色板及持久化 slot，P-03 走查同色和密集课表，P-09 实现并关闭 AC-15；任务状态不变 |
| 1.0.3 | 2026-09-04 | 同步 Java 选项 A：P-02 准备 JDK 21 并验证 JAVA_HOME、编译及 Maven 运行时；记录 Maven 3.9.16 已达标，Java 安装及其余任务状态不变 |
| 1.0.4 | 2026-09-04 | 同步 MySQL 选项 A：P-02 新建独立 8.4 开发及测试环境，保留本机 8.0.27，补充连接身份、服务器版本及隔离验证；不改变任务完成状态 |
| 1.0.5 | 2026-09-04 | 同步 Docker 选项 A：P-02 核验并准备 Docker Desktop/WSL 2，区分持久开发容器与临时测试容器，补启动、连接及清理验证；任务完成状态不变 |
| 1.0.6 | 2026-09-04 | 用户确认独立交互稿，统一根目录 design.md 与 interaction-design.md 路径；P-02/P-03 记为进行中并列明已交付和未验证项，保留任务依赖 |

实现时发现需求变化，先更新 requirement.md 的正式版本、关联设计和本计划，再修改实现。任务依赖改变时必须重新检查不存在循环，以及所有 REQ 和 AC 仍有负责任务。不将本轮文档保存描述为完成业务开发、通过兼容测试或部署上线。
