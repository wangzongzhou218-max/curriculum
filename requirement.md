# 选课系统需求规格说明书

> 文档版本：1.0.6  
> 状态：正式需求基线，业务范围与全部默认规则已由用户确认  
> 编写日期：2026-09-04  
> 原始来源：[requirement-temp.md](requirement-temp.md)  
> 配套任务计划：[development-plan.md](development-plan.md)，v1.0.6，与本需求同步  
> 配套设计：[design.md](design.md) 为技术契约，[interaction-design.md](interaction-design.md) 为交互契约，均与本需求同版。  
> 确认依据：用户明确采用 v0.2 的全部“建议默认”规则，并要求形成正式版及同步任务计划。  
> 使用方式：本文件是设计、实现和验收的业务依据；技术设计任务按计划先行，不将尚未执行的兼容性验证视为已通过。

## 1. 目标与规格驱动流程

大学开学前，教师录入课程及每周上课时间，新生选择课程并查看自己的周课表。系统通过管理员、教师、学生三种账号隔离功能。管理员维护师生账号，教师按学期管理课程并查看学生选课情况，学生在每学期最多选择四门课程的限制内安排无冲突的课表，并可退课或换课。

本文采用 SDD，即规格驱动开发：先约定可观察的业务行为和验收标准，再完成交互、接口及数据设计，最后按任务实现并用验收证据验证。需求说明回答“系统必须做到什么”，后续设计回答“如何实现”。设计不得擅自改写需求，变更应同步更新验收标准和任务引用。

### 1.1 正式基线与规范用语

原始草稿、后续范围修订及 v0.2 的全部默认规则已合并为本正式基线。正文中的“必须”“不得”“仅允许”是强制约束；“可以”“允许”表示系统必须支持的合法能力；“默认”表示已确定的初始值或默认行为，不表示可由开发人员自由选择。

本文件不存在需要再次确认的业务默认项。账号字段、身份场景、学期时间、课程状态、删除关联处理、退换课语义和非功能目标均已固定。接口路径、事务实现、依赖精确版本和性能测试环境属于技术设计交付内容，按第 13 节及任务计划完成，不重新讨论已确认的业务规则。

### 1.2 版本与追踪

需求编号 REQ、业务不变量 INV、验收标准 AC 的原有主题和编号全部保留。本文共 53 项需求、16 条业务不变量和 38 项验收标准；后续新增内容使用下一个未使用编号，不复用旧编号。

用户在本次对话中的采纳即构成 v1.0.6 业务定稿依据，不再要求重复确认。配套计划以 requirement.md v1.0.6 为固定来源；后续修改必须先按第 14 节记录需求变更，再同步设计、任务及验收。

## 2. 范围与成功标准

### 2.1 首版范围

首版包含三个子系统：

- 管理员：使用 admin 账号及默认密码 admin 登录管理员系统；对教师、学生账号进行新增、查询、编辑、删除。
- 教师：按学期创建、查看、编辑、删除、上架和下架本人课程；查看本人课程的学生名单；查看所有学生的姓名和按学期区分的选课情况。
- 学生：按学期浏览已上架课程、选课、退课、换课，查看本人已选课程和周课表。

每学年包含两个学期，分别于当年 9 月 1 日、次年 3 月 1 日开课，每学期持续十二周。原有师生自助注册仍保留，管理员创建账号是新增入口。具体时间区间见第 4.4 节。

课程保存后处于“未上架”状态，上架后才进入学生课程目录；下架只停止新的选课，不取消已有选课。学生每个学期最多选四门，跨学期分别计算。范围内包含历史学期查询，不包含单双周、节假日调课、教室或容量模型。

完整闭环为：管理员维护师生账号 → 教师选择学期录入并上架课程 → 学生选择该学期课程并查看课表 → 学生按规则退课或换课 → 教师查看课程名单与全体学生选课情况。

### 2.2 首版不包含

- 额外管理员账号管理、学校统一身份认证、教师身份审核流程。
- 邮箱验证码、师生自助找回或修改密码、短信或邮件通知；管理员为师生设置或重置密码在范围内。
- 容量、候补队列、学分、先修课、成绩、缴费及独立配置的选课开放窗口。
- 自动排课、批量导入导出、批量强制退课。
- 移动客户端、离线操作、多学校、跨学期换课、自动复制上学期课程。

课程编辑、删除、上下架、学生退换课以及教师学生查询已经纳入首版，不再列为后续可选功能。对于关联数据的处理，执行下文规定的关联数据规则。

### 2.3 成功标准

- 管理员、教师和学生均可独立完成各自的业务闭环。
- 所有写入路径均遵守角色限制、课程时间规则和学生选课规则。
- 页面刷新、重新登录及服务重启后，已成功提交的数据仍可正确查询。
- 每项原始要求与用户修订均有明确验收标准；需求、验收与任务的追踪关系见第 12 节，失败场景也有可观察结果。

## 3. 术语、身份与权限

### 3.1 术语

| 术语 | 定义 |
| --- | --- |
| 登录账号 | 教师和学生为系统随机生成的一位字母加七位数字；管理员为固定账号 admin |
| 工号／学号 | 自助注册时由系统生成，与登录账号为同一值；管理员建号仍可维护资料编号 |
| 学期 | 从固定开课日起连续十二周的教学周期，课程、选课及课表的数据归属单位 |
| 课程 | 某教师在某个学期开设的一门完整课程，是该学期“四门”上限的计数单位 |
| 周授课安排 | 每周某一天的一段连续上课时间，不是固定 30 分钟的一节课 |
| 周课表 | 所选学期的重复周视图；展示该学期起止日期，仅在学期有效期内产生实际授课 |
| 有效选课 | 尚未退课、换出或因账号删除取消的选课关系；历史学期的最终结果保留 |
| 可选课程目录 | 所选学期已上架且未删除的课程；已选课程标为“已选” |
| 全部学生 | 系统所有未删除的学生账号，包含尚未选课以及未选过该教师课程的学生 |

### 3.2 权限矩阵

| 操作 | 未登录 | 管理员 | 教师 | 学生 |
| --- | --- | --- | --- | --- |
| 师生注册、统一登录 | 允许 | 切换账号前先退出 | 切换账号前先退出 | 切换账号前先退出 |
| 本人身份、退出登录 | 拒绝 | 允许 | 允许 | 允许 |
| 师生账号增删改查 | 拒绝 | 允许，不能借此修改管理员账号 | 拒绝 | 拒绝 |
| 读取学期列表及起止日期 | 拒绝 | 允许 | 允许 | 允许 |
| 课程创建、编辑、删除、上下架 | 拒绝 | 不提供课程管理功能 | 仅本人课程，遵守生命周期规则 | 拒绝 |
| 教师课程管理列表及课表 | 拒绝 | 拒绝 | 仅本人课程 | 拒绝 |
| 浏览学生课程目录 | 拒绝 | 拒绝 | 不进入学生子系统 | 允许 |
| 选课、退课、换课 | 拒绝 | 不代替学生操作 | 不代替学生操作 | 仅本人 |
| 查看本人已选课程及课表 | 拒绝 | 拒绝 | 不进入学生子系统 | 仅本人 |
| 某门课程学生名单 | 拒绝 | 本期不提供专用入口 | 仅本人创建课程的名单 | 拒绝 |
| 全部学生姓名和选课情况 | 拒绝 | 本期账号管理只包含账号资料 | 允许，按学期查询 | 拒绝 |

REQ-01：账号角色为管理员、教师、学生，各自登录对应子系统。师生角色互斥；admin 是管理员身份，不遵循师生的字母加七位数字格式。

REQ-02：服务端根据已认证身份确定角色和操作权限。管理员可指定被管理的师生账号，教师可在授权的学生查询入口指定学生或本人课程，但这些参数不能改变操作者身份；学生只能操作本人选课。前端路由限制不能替代接口权限检查。

教师查看全部学生选课情况是明确授权的只读能力，并不授予编辑他人课程、查看他人课程专用管理名单或替学生选退课的权限。管理员账号管理可读取工号/学号及邮箱；教师学生查询只返回姓名、用于区分同名学生的登录账号和必要的选课信息，不返回邮箱、工号/学号、密码或会话信息。

## 4. 注册、登录和会话

### 4.1 注册

REQ-03：注册使用邮箱、密码和确认密码；两次密码一致才可成功注册。教师账号以 t 开头，学生账号以 s 开头，后跟恰好七位数字；系统随机生成且不能与数据库中已有账号重复。该生成账号同时作为工号／学号，是登录使用的唯一账号。

REQ-04：注册页面先选择教师或学生身份，再输入姓名、邮箱、密码、确认密码，不要求填写工号／学号。角色决定账号前缀，不能通过任意输入账号前缀获取权限。注册成功后清晰显示系统生成的工号／学号（即登录账号），提供复制操作，提示使用该账号登录；不自动登录。

| 字段 | 字段规则 |
| --- | --- |
| 角色 | 必填，只能为教师或学生；注册后不能自行切换 |
| 姓名 | 必填，去除首尾空白后 1～50 个字符；允许同名，按纯文本显示 |
| 工号／学号（登录账号） | 系统生成，不在注册表单填写；格式为 ^[ts][0-9]{7}$，前缀由角色决定，登录账号与工号／学号为同一值 |
| 邮箱 | 必填，去除首尾空白后最长 254 个字符；校验常规邮箱格式；首版统一小写后全局唯一 |
| 密码 | 必填，8～64 个字符，不全为空白；保持原值，不裁剪；不强制特殊字符组合 |
| 确认密码 | 必须与密码逐字符一致；不持久化 |
| 登录账号 | 仅服务端生成，格式为 ^[ts][0-9]{7}$；允许数字部分前导零 |

REQ-05：注册账号（同时为工号／学号）和邮箱唯一性由持久化约束保障。并发注册时也不得生成重复账号；随机账号碰撞时内部重新生成，最多尝试 10 次，仍失败则返回可重试错误，不能留下半注册状态。普通用户不能因碰撞获知已有账号资料。

注册字段不合法时指明对应字段；编号或邮箱重复时提示不可用。注册成功返回的账号丢失时，首版不提供自助找回功能，由管理员通过账号查询协助找回登录账号，必要时重置密码；不能查看原密码。

**身份与使用场景**：首版定位为受控演示系统，保留教师和学生自助选择角色注册，以及管理员创建师生账号。工号/学号是资料及唯一性字段，不承担真实身份认证。学校统一身份认证和真实师生资格校验不属于首版，不构成本版开发的未决事项；后续扩大到真实学校公开使用时，必须先通过需求变更新增相应能力。

### 4.2 登录与退出

REQ-06：使用登录账号和密码登录，登录后进入账号所属子系统；不能进入另一角色的系统。

REQ-07：采用统一登录入口。账号去除首尾空白后，仅接受 admin 或符合师生小写格式的账号，不接受邮箱或注册编号替代账号登录。账号不存在与密码错误使用同一提示“账号或密码错误”，不返回账号是否存在的判断。

REQ-08：刷新页面保留有效会话，退出登录后当前会话立即失效，后退或重用旧会话不能继续访问受保护数据。会话连续 30 分钟无操作或建立满 8 小时后失效，以先到者为准；自动后台轮询不延长无操作期限。失效后引导重新登录，不能假装原提交已成功。

账号已删除或密码被管理员重置时，该账号全部已有会话立即失效。会话存储、Cookie 配置、密码散列算法和 CSRF 防护由后续技术设计定稿。密码不得明文存储或记录日志；前端不持久保存密码、确认密码。

### 4.3 管理员登录与师生账号管理

REQ-38：系统预置唯一管理员账号 admin，默认密码为 admin。在统一登录入口完成账号和密码验证后，跳转管理员系统；输入账号字符串本身不构成登录成功。师生不能进入管理员系统，公开注册接口不能创建管理员。

管理员只在首次初始化且账号不存在时创建，密码使用与其他账号相同的安全散列存储。admin 的五字符初始密码是普通密码长度规则的明确例外；它不是所有教师、学生的默认密码。服务重启不能重复创建管理员或重置已有密码；管理员无须工号/学号和邮箱，不占用师生字段的唯一值。账号管理中不得删除、改名或更改 admin 的角色。管理员密码的部署配置及维护方式由技术设计约定，首版不要求首次登录强制改密。

REQ-39：管理员可以对教师和学生账号进行增删改查。

| 操作 | 输入与行为 | 成功结果 |
| --- | --- | --- |
| 新增 | 选择教师/学生，填写姓名、工号/学号、邮箱、密码及确认密码；复用师生校验和唯一约束 | 生成对应 t/s 账号，显示账号资料，该用户可登录 |
| 查询 | 按角色筛选，以姓名、登录账号或注册编号查询；默认创建时间倒序、同时间按内部标识排序；分页默认 20、最大 100 | 显示姓名、账号、角色、工号/学号、邮箱及创建时间；详情不返回密码或密码摘要 |
| 编辑 | 可修改姓名、工号/学号、邮箱；可单独输入新密码与确认密码进行重置 | 全部字段校验成功才提交；密码留空表示不变；重置密码使旧会话失效 |
| 删除 | 展示目标姓名、账号及关联影响，用户明确确认后提交 | 按 REQ-40 处理关联数据并立即阻止该账号登录 |

登录账号、内部标识及角色创建后不可编辑，避免工号/学号、账号前缀及历史归属混乱。修改姓名不改变课程、选课关联，后续查询展示新姓名。重复字段编辑失败时保留原资料；管理员新增与自助注册并发时遵守同一唯一性规则。

REQ-40：账号删除采用逻辑删除，不能直接清空有关联的历史数据。

- 删除学生：原子地取消其所有尚未结束学期的有效选课，更新对应课程名单及计数；已结束学期结果保留并标记“账号已删除”。成功后该学生不进入“全部学生”的日常列表，历史课程名单仍可呈现其历史身份。
- 删除教师：如果仍拥有未结束学期的未删除课程，拒绝并提示需先处理课程；已有选课的课程仍遵守 REQ-45，不能为删账号强制清空学生选课。只有历史课程时允许逻辑删除，保留课程归属和历史记录。
- 删除已删除的同一账号视为已完成；保留账号、编号、邮箱的唯一值，避免新账号继承旧身份，本期不提供恢复或复用入口。
- 删除、注册、选课及密码重置并发时，以持有同一业务写锁期间的状态复核保证已删除账号不能再产生新的业务写入；失败不能只删除账号而遗漏选课取消。
- 账号删除、密码重置需要记录操作者、目标、时间和结果，不记录密码；删除必须通过目标用户的版本校验防止误删已被他人修改的资料。

### 4.4 学期定义与访问边界

REQ-41：每个学年包含秋、春两个学期：学年起始年 Y 的秋季学期于 Y 年 9 月 1 日开课，春季学期于 Y+1 年 3 月 1 日开课；每个学期持续十二周。例如“2026—2027 学年秋季”和“2026—2027 学年春季”。

时区为 Asia/Shanghai，以开课日 00:00 为包含的起点，以起点加 84 个自然日的 00:00 为不包含的终点，即 [开课日, 开课日＋84天)。结束日期展示为终点的前一天。例如 2026 秋季覆盖 2026-09-01～2026-11-23，2027 春季覆盖 2027-03-01～2027-05-23。

十二周从固定开课日计时，不顺延到下周一。课程只在该区间内的相应工作日授课；开课日若为周末，第一次实际授课是区间内下一个匹配的工作日，不延长结束日期。节假日不自动调课。按完整学期的重复模板计算，每条周授课安排在 84 天内恰好对应十二次，两条安排共二十四次；中途创建或选择课程时，实际剩余授课按 REQ-42 处理，不补排过去日期。无需维护第十三周。

REQ-42：系统按规则生成当前学年和下一学年的四个学期，并保留已经生成的历史学期；学年在每年 9 月 1 日切换。首次启动、跨学年后访问时均可得到应有学期，重复生成不能创建重复学期。本期不提供任意修改开课日或时长的管理入口。

所有课程查询、创建、学生选退换课和教师学生选课查询必须携带明确学期上下文。页面默认选择正在进行的学期；没有正在进行的学期时，选择最近一个尚未开始的学期。用户可切换系统已有的未来和历史学期，空学期也需显示“暂无课程/未选课”。

每门课程仅归属一个学期，创建后不可更改学期。不同学期同名课程是不同课程编号，不自动复制，也不自动延续学生选课。

| 学期状态 | 判定 | 允许操作 |
| --- | --- | --- |
| 未开始 | 当前时间早于开课时间 | 教师创建、管理课程；学生对已上架课程选课、退课、换课 |
| 进行中 | 开课时间 ≤ 当前时间 < 结束边界 | 同上，课程时间编辑仍受已有选课限制 |
| 已结束 | 当前时间 ≥ 结束边界 | 课程、选课和课表只读，拒绝在结束后才进行锁内判定的新变更；结束前已通过判定的事务按下文规则完成 |

首版不另设提前选课截止日；未开始和进行中均允许选退换课，这是首版固定规则。进行中新增或选择课程只产生剩余时间内的实际授课，不补上已过去的授课，也不从创建或选课时间重新计算十二周。已结束学期不再接受新变更，结束前已通过判定的事务全部提交或回滚后，最终有效选课计数冻结，退课/换出记录作为操作历史保留。所有时间权限以服务端锁内判定时刻 decisionTime 为准：取得业务写锁、读取相关状态并完成账号、权限、版本及业务前置校验后，在实际领域写入前读取数据库 UTC 时间，再据此判断学期是否结束。判定时刻早于结束边界的操作允许完成，物理提交可以晚于边界；判定时刻等于或晚于结束边界则拒绝新变更。排队请求取得锁后必须重新取时，不使用点击、到达或排队前的时间。事务超时或失败仍须回滚，只有实际提交成功后才返回成功；回滚后的重试重新判定，已提交成功的同键重放只确认原结果。页面旧状态不能绕过此规则。

## 5. 教师课程功能

### 5.1 添加课程

REQ-09：教师点击“课程添加”，填写课程名称、课程描述，选择每周开始和结束时间，保存课程。

REQ-10：表单包括一至两条周授课安排，每条明确选择星期、开始时间和结束时间，默认一条，可添加第二条或移除第二条。必须选择一个未结束学期，服务端生成课程编号、绑定当前教师和学期，初始状态为未上架。名称与描述按纯文本处理，不允许输入内容成为可执行脚本。

| 字段 | 字段规则 |
| --- | --- |
| 所属学期 | 必填，只能选择未开始或进行中的学期；保存后不可跨学期迁移 |
| 课程名称 | 必填，去除首尾空白后 1～100 个字符；允许不同课程重名 |
| 课程描述 | 必填，去除首尾空白后 1～2000 个字符；保留正常换行 |
| 课程编号 | 系统生成，系统内全局唯一、不可修改，例如 C001；十进制正整数部分至少三位，不以三位限制容量 |
| 教师归属 | 服务端从当前会话取得，不接受客户端指定其他教师 |
| 周授课安排 | 必须一条或两条；两条不得在同一天；按星期、开始时间排序显示 |

REQ-11：所有课程仅在周一至周五的 08:00～12:00 或 14:00～18:00 内上课，每门课程每周最多两次，且不能在同一天。至少一次且对齐半小时是正式约束。

REQ-11A：教师新增课程时，必须与本人同一学期所有未删除课程逐条比较授课安排；任意安排的星期相同且时间区间重叠时，拒绝创建且不产生课程或安排。相邻的结束与开始时间不冲突。页面在新增课程弹窗中显示冲突课程及提示，保留用户已填写内容以便继续修改。

一条授课安排必须同时满足：

1. 星期为周一至周五。
2. 开始、结束均为当天时间，分钟只能为 00 或 30。
3. 开始时间严格早于结束时间，最短 30 分钟。
4. 整条安排位于一个允许时段内：08:00 ≤ 开始 < 结束 ≤ 12:00，或 14:00 ≤ 开始 < 结束 ≤ 18:00。
5. 不能跨午休、跨天或包含周末。一条上午安排和一条下午安排若在同一天，同样不允许。

合法示例：周一 08:00～10:00；周三 14:00～18:00。非法示例：07:30～08:30、11:30～14:30、17:30～18:30、09:15～10:00、两条均在周二。

REQ-12：课程主体和全部授课安排作为一次提交保存，任何一条失败都不得创建课程或留下孤立安排。成功后显示课程编号，并在对应学期教师列表、教师课表中可见，未上架状态明确标记；只有上架后才在该学期学生目录中可见。

同一保存操作的重复请求不得生成两门课程；对内容相同但用户明确新发起的保存操作不按名称去重。去重标识、保留时长、超时查询方式在技术设计中确定。

### 5.2 查看课程

REQ-13：教师点击“课程查看”，按所选学期看到本账号创建的全部未删除课程，并可点击“查看日历”进入周课表。

REQ-14：列表至少显示课程编号、名称、学期、上架状态、有效选课人数、完整周授课安排及详情入口；详情展示课程描述及可用管理操作。默认按创建时间倒序，同时间以稳定标识排序。分页时应能访问全部记录，不能仅显示第一页却宣称是全部课程。

教师无课程时显示空状态和“添加课程”入口。只允许读取本人课程管理详情，修改请求参数不能取得其他教师的私有管理数据。

同一教师不同课程允许时间重叠；课表必须同时展示这些课程，不得相互遮挡。

### 5.3 编辑、上架、下架与删除课程

REQ-43：教师可以编辑本人未结束学期的课程名称、描述及周授课安排，编辑校验复用创建规则。编号、教师归属和学期不可编辑。任何编辑必须一次性成功，失败保留全部原数据；编辑名称或描述不改变上架状态，相关列表、课表和学生选课详情下次查询展示最新内容。

课程存在有效选课时，允许改名称和描述，但拒绝修改上课星期、时间及安排条数；需先通过正常退课/换课使有效选课人数归零，再改时间。没有有效选课时，即使已上架也可编辑时间，但必须与选课并发互斥：编辑先提交则新选课按新时间校验，选课先提交则时间编辑拒绝。不能改时间后留下学生冲突，也不能绕过规则强制退课。

REQ-44：教师可以对本人未结束学期的课程上架、下架。状态和转换定义如下：

| 当前状态 | 操作 | 下一状态及效果 |
| --- | --- | --- |
| 未上架 | 上架 | 已上架；合法且未删除时进入所选学期学生课程目录 |
| 已上架 | 下架 | 已下架；退出学生课程目录，拒绝新的选课和作为换入课程 |
| 已下架 | 上架 | 已上架；恢复可选，保持同一课程编号及原有效选课 |
| 未上架、已上架、已下架 | 删除 | 无有效选课且学期未结束时可进入已删除；否则拒绝 |
| 已删除 | 编辑、上下架、选课 | 拒绝，不提供恢复入口 |

下架保留现有有效选课、已选数量、学生课表和课程学生名单；已有学生仍可在学期未结束时退课或换出。学生本人详情仍可查看下架的已选课程，标明“已下架，已有选课保留”。教师周课表显示所有未删除课程并标注状态。重复请求已经达到的上下架状态返回当前结果，不能重复触发附带写入。

REQ-45：教师可以删除本人课程。删除前展示课程编号、名称、学期及影响并要求确认；仅允许删除未结束学期、有效选课人数为零的课程。

存在有效选课时返回“该课程仍有学生选课，无法删除”，不得隐式取消学生选课；已结束学期课程只读。删除采用逻辑删除，从日常课程列表、课程目录和教师课表移除，保留编号、授课安排快照及退换课操作历史，不复用编号。重复删除同一已删除课程返回已删除结果，仍需验证原教师归属。删除与选课同时发生时，不能同时得到“删除成功”与有效新选课。

### 5.4 查看本人某门课程的学生名单

REQ-46：教师在本人课程列表或详情中点击“学生列表”，查看该课程的学生名单。入口明确显示学期、课程编号、课程名称和有效选课人数。

列表按学生登录账号升序分页，展示姓名、学生登录账号、选课时间；仅包含该课程当前有效选课，历史学期则展示结束时保留的最终名单。未上架无选课课程显示空列表，已下架课程仍显示保留名单。退课、换出和未结束学期的学生账号删除后，该学生从名单及人数中移除。历史名单内被删除账号显示“账号已删除”，不影响人数历史结果。

只能通过该入口查询本人创建课程的名单，篡改课程编号访问其他教师课程必须拒绝，即使教师拥有全体学生选课概览权限也不放开此管理接口。

### 5.5 查看全部学生及其选课情况

REQ-47：教师系统增加“学生总览”，可查看系统全部学生，至少包含姓名和选课情况；不能只列出选过该教师课程的学生。总览支持从本人当前学期课程名称下拉框选择一门课程，筛选出已选择该课程的全部学生；服务端只接受本人课程作为筛选条件。

按所选学期显示所有未删除学生的姓名、学生登录账号、已选门数和完整已选课程清单；每门课包含编号、名称、任课教师姓名、周授课安排、上架状态。未选课显示“0 / 4，未选课”。课程来自所有教师，不按当前教师归属缩减；下架但仍有效的选课也计入。历史学期查询显示该学期最终结果，学期更换不得把多个学期混成一个总计。

支持姓名或学生登录账号查询，以及全部/已选课/未选课筛选；默认按学生登录账号升序分页。每个学生在单个学期最多四门，清单可以展开但不能静默截断。对姓名相同的学生以登录账号区分。该页面完全只读，不提供账号编辑、代选、退课、换课及获取其他学生登录凭证的入口。

## 6. 学生选课功能

### 6.1 浏览课程

REQ-15：学生点击“课程选择”，从含课程名称的列表中选择课程。

REQ-16：可选列表展示所选学期全部已上架且未删除的课程，至少包括课程编号、名称、全部授课时间及描述入口，默认按课程编号的数字部分升序。已上架的已选课程保留在目录中并标为“已选”；下架后的已选课程只在本人已选列表与课表中保留，不得重复产生选课关系。目录分页不能影响总数限制或冲突判断。

课程详情及选课操作不能泄露教师的邮箱、注册编号等注册资料。可用性提示仅作参考，最终结果以服务端锁内校验及第 4.4 节的 decisionTime 判定为准，实际提交成功后才返回成功。

### 6.2 选课规则与提交

REQ-17：同一学生每个学期最多有效选择四门课程，同一学期所选课程之间时间不得冲突；不同学期分别计数和检查。

REQ-18：一门课程无论每周上课一次还是两次，只计为一门；选择课程意味着同时选择该课程的全部授课安排，不支持选择其中一次。每次提交只选择一门课程，不做多门批量提交。

REQ-19：将授课时间视为左闭右开区间 [开始, 结束)。两条安排仅当星期相同且“甲开始 < 乙结束，同时乙开始 < 甲结束”时冲突。对候选课程的每条安排，与同学期全部有效已选课程的每条安排逐一检查；任意一对冲突即拒绝整门课程。

因此周一 08:00～09:00 与周一 09:00～10:00 不冲突；周一 08:00～09:30 与周一 09:00～10:00 冲突；不同星期的相同时间不冲突。

REQ-20：服务端按以下顺序处理：认证与账号有效性 → 输入、学期与课程存在性及对应关系 → 识别已成功请求的重放 → 是否已有有效选课 → 学期是否结束与课程是否已上架 → 该学期是否已满四门 → 时间冲突 → 写入。已选同一课程时返回当前已选结果，不新增记录，不因已有四门而把重复提交错误地视为第五门；课程后来下架或学期结束时，确认已有结果也不产生新写入。已退课后的再次选课是新操作，必须重新通过所有条件。

REQ-21：所有检查与选课写入必须具有一致的事务保障。同一学生同时选择两门课时，最终结果仍然在各学期内不超过四门且无冲突；界面按钮禁用不能替代服务端约束。有效选课关系对“学生＋课程”唯一；退换课历史独立保留，重新选入不会产生两条有效关系。

超过上限时提示“本学期最多选择四门课程”；冲突时至少指出冲突课程编号、名称、星期和时间；课程不存在时提示不可用并刷新目录。失败均不得新增选课关系。

REQ-22：成功后更新已选计数、课程状态、已选列表和课表。请求超时不能直接认定失败并引导重复创建；应提示结果待确认，重新查询本人已选课程。失败不清空已有选课。

### 6.3 已选列表

REQ-23：学生点击“课程查看”，显示本账号在所选学期仍然有效的全部已选课程，包含已下架但保留选课的课程；点击“查看日历”显示这些课程的周分布。

REQ-24：列表至少显示学期、课程编号、名称、上架状态、全部上课时间和描述入口，同时显示“本学期已选 X / 4 门”；未结束学期提供退课、换课操作，已结束学期只读。列表默认按课程编号数字部分升序。未选课时显示空状态和“去选课”入口，不能混入课程目录中的未选课程。

### 6.4 退课与换课

REQ-48：学生可对本人未结束学期的有效选课执行退课。确认页展示学期、课程编号和名称；成功后该课程不再占用四门名额，从本人已选列表、课表、该课程学生名单和学生总览的有效选课中移除。已下架课程也可退课，已结束学期不允许退课。

退课记录保留供结果确认及历史核对，界面只把有效关系计入已选。重复提交同一次退课返回原结果，不再次修改数据；对于从未属于本人或已失效的关系，不得影响其他选课。退课后若课程仍已上架且学期未结束，可通过一次新选课重新加入。

REQ-49：学生可将本人一门有效已选课程换为**同一学期**另一门已上架且未删除的课程；来源课程即使下架也允许换出，目标课程必须可新选，学期必须未结束。

换课检查先从该学期当前有效选课集合中暂时排除来源课程，再加入目标课程，验证替换后的数量与时间冲突。目标与被换出课程重叠不应单独导致失败，但与剩余任一已选课程冲突必须拒绝。目标已是另一门有效已选课程时拒绝，避免把换课变成少选一门；来源与目标相同时返回“不需要换课”，不变更数据。

换出与换入是一个原子操作：全部成功才切换两门课的选课关系与人数。目标不合法、已下架、冲突、源选课已变化或事务失败时，保留原课程，不允许“先退成功、再选失败”。已选四门也可合法一换一，不按临时第五门拒绝。成功后刷新两门课程的名单、学生计数、已选列表、课表及学生总览。

REQ-50：选课、退课、换课、课程编辑及上下架/删除、账号删除与学期结束共享一致性边界。持有同一业务写锁时检查账号、课程状态及数据版本，按第 4.4 节的 decisionTime 检查学期；判定后持续持锁至提交或回滚，操作结果必须等价于某种顺序执行，禁止丢失其他成功操作。

- 所有写入携带操作标识；同一操作者、同一操作标识和同一内容重试仅确认一次结果。同一标识不同内容应拒绝；仍需验证操作者有效身份。
- 编辑、上下架、删除携带被操作资源版本；新操作使用旧版本时提示“数据已更新，请刷新”，不得覆盖新状态。
- 退换课绑定来源选课关系的版本或等效代次标识。已退后重新选入是新的代次，旧退课/换课请求重放不能取消这次新选择。
- 同一来源课程同时换向两个目标，只允许一个有效替换；删除和选课、下架和换入、删账号和选课等组合不能留下违反状态或归属的记录。
- 请求超时先显示待确认，通过操作结果和当前已选集合核实；不把第一次成功后的新状态再回放成一次业务操作。
- 锁、版本、请求标识保留期及结果查询接口由技术设计集中定义，并纳入真实 MySQL 的并发验收。

## 7. 周课表与页面交互

REQ-25：课表横轴为周一至周五，纵轴以半小时为间隔；按课程时间绘制色块，课程使用固定 12 色调色板，允许颜色复用，色块显示“课程编号-课程名称”，例如 C001-AI Coding 101。

REQ-26：课表显示 08:00～18:00 的连续时间轴，共 20 个半小时格；12:00～14:00 的四格标为午休且不放置课程。08:00、12:00、14:00、18:00 边界应清晰可读。

REQ-27：一条授课安排对应一个连续色块，同一课程每周有两条安排则产生两个同色块。色块顶部和底部准确对应开始和结束时间，不能把两条安排合并成跨天或跨午休色块。

REQ-28：采用固定 12 种高区分颜色的调色板，颜色不足时允许复用，不要求任意两门课程拥有不同颜色。配色绑定课程身份，不随当前用户、查询子集、分页、列表顺序、刷新或选退换课重新分配，因此一个查询子集内也可能出现同色课程。同一课程在列表、详情和课表间保持颜色及边框样式一致，两次授课安排使用同一套样式。结合课程编号、名称和实线/虚线/双线边框辅助区分；颜色和边框组合均可复用，不承诺视觉编码唯一，编号和文本始终保留。固定色值、分配规则及边框样式由技术设计明确，交互设计须走查超过 12 门课程、同色重叠、长名称和窄屏，不单靠颜色传达身份。

REQ-29：教师存在重叠课程时采用并列等可读布局展示所有色块，不允许后绘制的课程遮住前一课程。学生课表不应出现重叠安排；若后端发现异常数据，返回明确错误，不默默隐藏部分课程。

REQ-30：课程名称过长可截断，但鼠标、键盘或触摸操作均可查看完整名称、星期、起止时间和描述。列表和课表来自同一所选学期、权限范围和有效课程数据；教师查看本人未删除课程并标注上架状态，学生查看本人有效已选课程，包括保留的下架课程。

REQ-51：课程目录、本人列表、周课表、课程名单和学生总览均明确显示所选学期及起止日期，切换学期时清空旧学期视图状态并重新查询，不能把上次选择的课程提交到新学期。

周课表继续采用周一至周五的重复周模板；本期不强制提供具体日期的逐周导航，但必须标注“每周重复，仅在本学期起止日期内有效”。实际日期和十二周有效窗口按 REQ-41 计算，不能因页面使用周模板而跨期生成授课。教师未上架课程显示为备课安排，下架课程显示状态；学生退课或换出课程不再显示。

所有主要页面应区分加载中、无数据、校验失败、网络或服务失败、成功状态。表单失败保留非密码输入；密码在离开表单后清理。按钮提交期间避免重复点击，但重试安全仍由后端保证。

首版界面使用简体中文。桌面浏览器为主要目标，窄屏应可滚动或切换可读布局，不允许关键按钮或时间信息无法访问。

## 8. 概念数据与业务不变量

### 8.1 概念模型

本节约定业务含义，不提前指定具体表名或字段类型。

| 实体 | 最小信息 | 关系与约束 |
| --- | --- | --- |
| 用户 | 内部标识、登录账号、角色、姓名、注册编号、邮箱、密码摘要、账号状态、版本、创建/删除时间 | admin 为保留账号；师生前缀与角色一致；师生编号同角色唯一，邮箱全局唯一；逻辑删除不释放唯一值 |
| 学期 | 内部标识、学年起始年、秋/春类别、开课时间、结束边界 | 学年和类别组合唯一；固定开课日；持续 84 天 |
| 课程 | 内部标识、课程编号、学期标识、名称、描述、创建教师、上架状态、删除标记、版本、创建/修改时间 | 编号全局唯一；只归属一个教师和一个不可变学期；删除后保留历史 |
| 周授课安排 | 内部标识、课程标识、星期、开始时间、结束时间 | 每门课程 1～2 条；同课程的星期不可重复 |
| 选课关系 | 内部标识、学生标识、课程标识、有效状态、版本/代次、选课及失效时间 | 同学生与课程至多一条有效关系；所属学期从课程唯一确定；每学期最多四门且无冲突 |
| 操作记录 | 操作者、操作标识、操作类型、目标标识、学期、结果、时间及必要变更信息 | 支持重复请求确认、退换课和账号/课程管理历史；不记录密码和凭证 |

内部标识由服务端产生，不使用可修改的名称作为关联键。课程编号独立于名称，名称相同不影响身份。编号生成失败需失败返回，不能保存无编号记录。编号允许不连续，不能使用“现有记录数＋1”实现并发生成。

周授课时间使用学校本地的星期与时分语义，**时区为 Asia/Shanghai**，不得因客户端时区变化而改变周课表。创建、选课等审计时间的存储与展示方式由设计明确。

### 8.2 必须始终成立的规则

- INV-01：每个账号只有一种角色；师生前缀与角色匹配，admin 仅为管理员，所有账号全局不重复。
- INV-02：受保护操作只能访问当前角色和当前用户被允许的数据范围。
- INV-03：每门课程有且仅有一至两条合法安排，且星期互不相同。
- INV-04：任何授课安排不跨午休、不超出允许时段，且对齐半小时。
- INV-05：每个学生在每个学期最多有四个不同课程的有效选课关系。
- INV-06：每个学生在同一学期的有效已选课程任意两条安排不冲突。
- INV-07：相同学生和课程至多存在一条有效选课关系，重试不增加计数或撤销重新选入的新关系。
- INV-08：课程及授课安排整体成功或整体失败；选退换课检查与写入不可被并发绕过，换课失败保留来源课程。
- INV-09：列表、课表及名单按各自约定，在同一学期和权限范围内完整展示对应有效数据。
- INV-10：原始密码、确认密码、会话凭证不进入响应日志或公开课程数据。

- INV-11：每门课程归属且仅归属一个学期，跨学期不共享选课计数，不能跨学期换课。
- INV-12：每个学期从规定开课日起持续 84 天；decisionTime 等于或晚于结束边界时拒绝课程和选课的新变更。结束前已通过锁内判定的事务允许在结束后完成物理提交，失败仍回滚；已提交成功的结果允许查询和重放确认。
- INV-13：只有已上架、未删除、未结束学期的课程可产生新有效选课；下架保留已有有效选课。
- INV-14：有有效选课的课程不能删除或修改授课安排，课程与学生状态变化必须并发一致。
- INV-15：删除账号立即使其所有会话失效；未结束学期不产生引用已删除学生的新有效选课，历史归属不被清空。
- INV-16：教师专用课程名单限本人课程，学生总览明确允许读取全部学生的选课情况，但不能借此扩大写权限。

## 9. 失败行为、接口契约与生命周期

REQ-31：所有接口错误提供稳定的业务错误标识、用户可读提示和可用于排查的请求标识；校验错误可附字段名。禁止将数据库异常、堆栈、密码等直接返回前端。

以下是必须表达的业务结果，具体接口路径、HTTP 状态、数据结构在技术设计中集中定义：

| 情况 | 用户观察到的结果 | 数据结果 |
| --- | --- | --- |
| 注册字段非法、密码不一致 | 字段错误，仍可修正 | 不创建用户 |
| 注册编号或邮箱重复 | 对应信息不可用 | 不创建第二个用户 |
| 账号生成重试耗尽 | 暂时无法注册，可稍后重试 | 不留下半注册用户 |
| 登录失败 | 账号或密码错误 | 不建立有效会话 |
| 未登录或会话失效 | 要求重新登录 | 不执行受保护写入 |
| 角色不符或越权 | 无权访问 | 不返回私有资源内容 |
| 非法课程时间 | 指明不合法的授课安排 | 不保存课程及安排 |
| 重复选课 | 展示已选成功状态 | 仍然只有一条关系 |
| 第五门或时间冲突 | 上限或冲突原因 | 原选课保持不变 |
| 请求超时 | 结果待确认，支持重新查询 | 不承诺请求一定未提交 |
| 数据库暂不可用 | 服务暂不可用 | 不返回虚假成功 |
| 页面刷新或服务重启 | 可再次查询已成功提交的数据 | 已提交数据不得丢失，admin 不被重复初始化或重置 |
| 学期已结束、学期参数与课程不符 | 明确只读或学期错误 | 不修改课程或选课 |
| 下架或删除的目标被新选/换入 | 课程当前不可选 | 不新增关系，换课保留来源 |
| 删除或改时段遇到有效选课 | 明确已有学生选课 | 保留原课程与全部选课 |
| 换课部分步骤失败 | 换课失败，原课程保留 | 换入换出全部回滚 |
| 新操作使用旧版本 | 数据已更新，请刷新 | 不覆盖其他成功修改 |
| 删除教师仍有未结束学期课程 | 明确列出需处理的课程 | 保留账号及课程 |
| 删除学生或重置密码后继续请求 | 会话已失效 | 不允许旧会话读写 |

REQ-32：列表统一采用服务端分页，默认每页 20 条、最大 100 条，返回总数；本人周课表按同一用户和学期范围一次性取得全部有效周授课安排，不能只取列表当前页。管理员账号列表、教师课程名单和全部学生列表也执行相同分页规则；单个学生的所选学期选课清单必须完整。首版容量目标下保证完整，超过容量目标需先评估而非静默截断。

REQ-33：部署启动时若配置或数据库不可用，就绪检查应失败，不开放正常业务写入。运行中故障请求返回服务不可用，恢复后新请求可继续；关停时未完成事务按数据库事务语义回滚，已提交事务在重启后仍然可见。迁移、回滚及备份操作需形成操作文档，不在本轮执行。

REQ-52：账号及课程增删改、上下架、选退换课记录可追踪的业务结果和操作时间；学生、教师页面不得访问凭证或通用内部审计数据。教师全体学生查询只返回 REQ-47 授权字段，不能通过响应中的嵌套用户对象泄露邮箱、工号/学号或密码摘要。名单只读权限须在接口级单独授予。

REQ-53：如实现前已经存在旧版账号或课程数据，升级方案必须补全师生姓名、将课程与选课明确归入一个学期，并确定既有课程的上架状态。不允许根据创建日期静默猜测学期或把全部历史课当成当前课。当前仓库未包含业务实现或旧数据迁移来源，首版按空库初始化设计，不制造旧数据迁移任务。实施时如实际接入旧数据库，必须先形成显式映射和校验记录，再执行升级；这一条件规则已经确认，不是待决定的业务选项。

## 10. 技术约束与非功能要求

### 10.1 原稿指定技术栈

REQ-34：保留以下技术栈要求，不在扩充需求时擅自替换版本。

| 层次 | 原稿要求 | 技术设计必须验证的内容 |
| --- | --- | --- |
| 前端 | Nuxt 4.5.2、Vue 3、TypeScript、pnpm 11 | 版本可获取性、配套依赖与 Node.js 约束 |
| 后端 | Spring Boot 4.1.1、Spring Web MVC、Spring Data JPA | 版本可获取性、Java 与 Maven 兼容组合 |
| 运行环境 | Node.js 20.19+、Java 17+、Maven 3.9.16 | 用户已确认采用 Java 21 LTS；开发、编译、测试及部署统一使用 JDK 21，编译 release=21；P-02 固定具体发行版和补丁并验证兼容性 |
| 数据库 | MySQL | 用户已确认采用 MySQL 8.4 LTS；新建独立开发实例，保留本机现有 8.0.27 实例及数据；开发与隔离测试统一使用固定的 8.4 补丁版本，字符集、排序规则与隔离设置按技术设计执行 |
| 本地容器及测试环境 | 用户确认 Docker 选项 A | Docker Desktop 使用 WSL 2 后端运行 Linux 容器；开发 MySQL 8.4 独立持久化，集成测试通过 Testcontainers 创建和清理临时实例；P-02 核验本机条件并固定精确版本，不据此限定生产环境必须使用 Docker Desktop |

本轮未验证这些版本的发布情况及相互兼容性，不能把“写入需求”视为“兼容性已验证”。“20.19+”“17+”是下限表达，不代表所有大于该版本的组合都可用；若固定框架和包管理器要求更高运行版本，应在范围内选择并记录理由。若需要修改明确指定的框架或 Maven 版本，必须记录需求变更。

### 10.2 安全、可靠性与可用性

REQ-35：

- 服务端验证所有输入，持久化查询不得拼接不可信 SQL，文本显示需正确转义。
- 密码使用专用密码散列方案；认证凭证受保护；真实部署通过 HTTPS 传输。
- 有效认证不等于有权读取所有资源，每次访问仍检查角色与归属。
- 对登录、注册设置可配置的尝试频率限制，触发时返回明确提示且不产生部分写入；具体阈值、限制维度和测试方式由技术设计确定。
- 日志包含请求标识、操作结果、耗时及必要内部标识，排除密码、会话凭证和完整注册请求体。
- 密钥与数据库凭据不提交代码仓库；测试使用虚构注册信息。

REQ-36：容量目标为 1 名管理员、100 名教师、2000 名学生，每学期 1000 门课程、最多 2000 条周授课安排、50 名并发活跃用户。验收至少准备两个学期的数据，覆盖跨学期查询、全体学生选课清单、课程人数更新和退换课写入。在与部署目标相近的环境内，持续 10 分钟混合业务请求，常规列表和写入请求的服务端响应时间 P95 不超过 1 秒，预期业务拒绝不计为系统故障，非预期错误率低于 1%。设计必须明确硬件、数据分布、请求比例和测量工具，不能在条件不一致时宣称达标。

REQ-37：桌面至少验证 Chrome、Edge 的验收时稳定版，记录确切版本；验证 1366×768 和 390×844 视口。主要操作可通过键盘完成，表单有标签，错误提示可理解，课表不单靠颜色区分课程。主要业务页面在正常测试网络下首次可操作时间目标为 3 秒内，记录测试环境与缓存条件。

本节容量、响应时间、错误率、浏览器和可访问性目标均为已采纳的正式验收条件。测量环境由 P-02 固定、P-10 实测；任何放宽目标的行为都必须先走需求变更，不能以演示系统为由跳过。

## 11. 验收标准

每项验收均需保留实际结果。涉及服务端规则的用例要包含直接接口调用，不能只证明页面限制有效。

| 编号 | 场景与输入 | 预期结果 |
| --- | --- | --- |
| AC-01 | 分别注册教师和学生，填写有效姓名、编号、邮箱和相同的两次密码 | 分别得到 t/s 加七位数字的账号，含前导零格式合法；账号持久化且角色正确 |
| AC-02 | 密码不同、空编号、非法邮箱、重复编号或邮箱；强制随机账号碰撞及并发注册 | 非法数据不写入；唯一约束始终成立；账号碰撞自动重试，耗尽后无半成品 |
| AC-03 | 正确与错误密码登录；教师访问学生接口，学生访问教师接口 | 正确账号进入对应子系统；错误凭据统一提示；跨角色访问被服务端拒绝 |
| AC-04 | 刷新页面、退出后重用会话、模拟会话超时 | 有效会话可恢复；退出和超时后的会话不能访问受保护数据 |
| AC-05 | 教师在未结束学期保存含一条安排、含两条不同星期安排的课程 | 编号唯一、学期正确、数据完整；保存后教师可见且未上架，上架后学生目录可查询；两次安排仍是同一门课程 |
| AC-06 | 无安排、三条安排、同日两条、周末、跨午休、超时段、非半小时、结束不晚于开始 | 全部拒绝，指出原因，数据库无部分课程 |
| AC-07 | 08:00～08:30、11:30～12:00、14:00～14:30、17:30～18:00；08:00～12:00、14:00～18:00 | 均合法；12:00～14:00 不合法 |
| AC-08 | 两名教师各有课程，篡改用户或课程参数查看管理数据 | 每名教师仅获取自己的课程；不能由请求参数改变归属 |
| AC-09 | 学生打开所选学期已上架课程目录，包含重名、未选和已选课程 | 用编号正确区分；显示名称、完整时间和描述入口；已选状态准确 |
| AC-10 | 学生在同一学期依次选择四门无冲突课程，再提交第五门 | 前四门成功且计数正确；第五门拒绝，已有选择不变 |
| AC-11 | 候选课程第二次安排与已有课重叠；同日相接；不同日同时间 | 重叠拒绝并给出具体冲突；相接和不同日同时间允许 |
| AC-12 | 重复提交同一课程，含已选四门时重试；并发重复请求 | 均保持同一已选结果，数据库仅一条有效关系 |
| AC-13 | 同学期已有三门时并发选两门不同课程；同学期并发选择两门互相冲突的课程 | 前者最终不超过四门；后者至多新增其中一门；失败方收到明确结果 |
| AC-14 | 两名学生各自选课，互相篡改查询或提交中的学生标识 | 只能读写本人选课；已选列表与课表一致，目录不会混入已选视图 |
| AC-15 | 查看两次安排、长名称、超过 12 门教师课程及同色重叠；切换列表/详情/课表、分页并刷新，执行选退换课；覆盖桌面和窄屏 | 五天、20 格及午休正确；颜色来自固定 12 色表，允许复用；同课程颜色和边框稳定，同色课程保留可读编号名称及边框辅助信息；重叠可见，长文本可访问完整详情，不以颜色唯一作为验收条件 |
| AC-16 | 无课程、无选课、加载失败、课程保存失败、连续点击及写入请求超时 | 有清晰状态和恢复入口；无假成功；相同保存操作不创建重复课程；超时可确认实际结果 |
| AC-17 | 在注册、描述等输入脚本或注入字符串，检查日志与未授权响应 | 不执行脚本或不可信查询；凭证不泄漏；非公开资料不暴露 |
| AC-18 | 数据库中断、恢复、服务重启；课程写入中故障 | 未提交写入回滚，恢复后可重试；已提交数据保留；就绪状态与实际可用性一致 |
| AC-19 | 超过一页的课程数据、切换分页后查看课表 | 列表可遍历全部记录，排序稳定；课表包含本人全部安排，选课校验不受分页影响 |
| AC-20 | 固定兼容版本后按部署说明初始化环境并执行验证 | 前后端、数据库及迁移流程可用；版本、配置和复现步骤记录完整 |
| AC-21 | 执行容量和性能测试、两种视口及两个浏览器验收、注册登录频率限制测试 | 满足 REQ-35～37 及技术设计确定的测量条件；超限认证请求受控拒绝；报告包含环境与测量数据 |
| AC-22 | 首次初始化后使用 admin/admin 登录；错误密码、伪造 admin 字符串、师生访问管理接口；重复初始化或重启 | 正确认证进入管理员系统；其他请求拒绝；仅一个 admin，已有密码不被重置，公开注册不能创建管理员 |
| AC-23 | 管理员新增、查询、修改师生资料及重置密码；姓名为空、邮箱重复、非法角色、编辑 admin | 合法操作成功；t/s 账号格式正确；查询支持分页；非法修改无部分提交；原密码不可查询；重置后旧会话失效 |
| AC-24 | 删除有当前选课的学生、有未结束课程的教师、仅有历史课程的教师，随后重用会话并查询历史 | 学生当前选课原子取消；第二种删除拒绝；第三种逻辑删除成功；旧会话失效；历史归属与最终名单保留 |
| AC-25 | 生成 2026—2027 学年两学期，重复生成；验证开课日、结束日、十二周及周末开课 | 开课为 2026-09-01、2027-03-01；结束展示 2026-11-23、2027-05-23；区间各 84 天；一条安排十二次，不顺延学期、无重复学期 |
| AC-26 | 同一学生在秋、春学期各选四门且周时间相同；在其中一个学期尝试选择第五门；尝试跨学期换课 | 两个学期分别允许四门，不互相冲突；第五门仅在该学期被拒绝；跨学期换课拒绝 |
| AC-27 | 编辑本人未结束学期的课程名称描述及时间；有学生时改时间；编辑他人、历史课程或学期归属 | 合法内容更新且编号不变；有学生时仅允许名称描述修改；其余越界修改拒绝；失败时原值不变 |
| AC-28 | 课程保存、上架、选入、下架、再上架；下架后尝试新选和换入 | 初始未上架；目录只含已上架课程；下架后现有选课、计数和课表保留；新选与换入拒绝；再上架保留原课程身份 |
| AC-29 | 删除无有效选课课程、有有效选课课程、他人课程、已结束学期课程，重复删除 | 仅符合条件的本人课程逻辑删除；列表和课表移除；其他删除拒绝；编号和历史保留，重复操作无额外影响 |
| AC-30 | 退掉已选或已下架课程，重复同一次退课，重新选入，执行旧退课请求 | 合法退课释放名额并同步列表、课表及名单；可按规则重新选入；旧请求不能撤销新选择 |
| AC-31 | 已选四门时一换一；目标仅与来源时间重叠；来源已下架 | 替换后与其余课程无冲突即可成功；仍为四门；两门课程名单及学生视图同时更新 |
| AC-32 | 换入与其他课程冲突、目标已选/下架/删除/不同学期、来源已失效，或换课中注入故障 | 全部拒绝或回滚，失败保持原有效选课集合，不出现只退未选；来源与目标相同则无变更 |
| AC-33 | 同一来源并发换向两个目标；选课与改时间/下架/删除并发；学生删除与选课并发 | 最终结果等价于合法串行执行，不超额、不冲突、无被删除目标的新有效关系；各失败方有明确结果 |
| AC-34 | 教师查看本人上架/下架课程名单，无选课、退课、换出、删除学生及历史学生账号删除场景；请求其他教师课程专用名单 | 本人名单、人数与有效选课一致；历史保留被删除学生标记；其他教师专用名单拒绝访问 |
| AC-35 | 全体学生中有重名、未选课、只选其他教师课程、下架课程选课、多页数据，切换学期 | 教师能看到全部未删除学生姓名及完整当期选课；账号区分同名；0/4 明确；不泄露邮箱、注册编号或密码，不允许代操作 |
| AC-36 | 覆盖结束前锁内判定、判定后暂停跨边界提交、排队跨边界后判定、恰好边界判定、回滚后跨边界重试及成功结果重放；切换历史/未来学期；进行中选入课程 | 结束前通过判定且事务最终成功则允许，即使实际写入和提交晚于边界；排队后或重试时判定已到边界则拒绝新变更；失败整体回滚，成功重放不再写入；历史可查，不延长十二周，切换不混入其他学期 |
| AC-37 | 重复编辑、换课和账号管理请求；相同操作标识不同内容；过期资源版本；响应丢失后查询 | 同操作仅一次结果；内容冲突与旧版本新操作拒绝；可确认结果，不能覆盖后来修改或误取消重新选入 |
| AC-38 | 空库初始化；如存在旧数据，先检查并执行明确的迁移映射 | 空库按新版生成；有旧数据时姓名、学期、上架状态均有明确映射及校验证据，不静默猜测或丢失关系 |

## 12. 追踪关系与验证策略

[development-plan.md](development-plan.md) v1.0.6 与本需求同步。P-01 记录已完成的业务基线定稿；P-02、P-03 完成技术与交互设计；其余任务按依赖交付可验证的业务结果。旧计划的范围排除条款已被本版计划替代。

| 要求主题 | 对应需求 | 验收标准 | 对应任务 |
| --- | --- | --- | --- |
| 三角色身份、注册、会话 | REQ-01、REQ-02、REQ-03、REQ-04、REQ-05、REQ-06、REQ-07、REQ-08、REQ-38 | AC-01～04、17、22 | P-04 |
| 管理员师生资料及删除 | REQ-39、REQ-40 | AC-23～24、37 | P-12、P-18 |
| 学期生成与访问边界 | REQ-41、REQ-42 | AC-25～26、36 | P-11；P-05、P-08、P-13～18、P-09 落实各写入边界 |
| 教师创建与本人查询 | REQ-09、REQ-10、REQ-11、REQ-12、REQ-13、REQ-14 | AC-05～08、16、19 | P-05、P-06 |
| 课程编辑、上下架、删除 | REQ-43、REQ-44、REQ-45 | AC-27～29、33、37 | P-14、P-13、P-15 |
| 学生目录、选课、本人列表 | REQ-15、REQ-16、REQ-17、REQ-18、REQ-19、REQ-20、REQ-21、REQ-22、REQ-23、REQ-24 | AC-09～14、16、19、26、28 | P-07、P-08 |
| 学生退课、换课 | REQ-48、REQ-49 | AC-30～32、36～37 | P-16、P-17 |
| 两类教师学生查询 | REQ-46、REQ-47 | AC-34～35 | P-19、P-20 |
| 双端学期周课表 | REQ-25、REQ-26、REQ-27、REQ-28、REQ-29、REQ-30、REQ-51 | AC-15、19、25、28、30～31、36 | P-09；P-06～08、P-19～20 负责各自学期列表 |
| 错误、分页、运行恢复 | REQ-31、REQ-32、REQ-33 | AC-16、18～20 | P-02 统一契约；P-04～20 在各自范围落实；P-10 汇总恢复验收 |
| 技术栈、安全、性能、可访问性 | REQ-34、REQ-35、REQ-36、REQ-37 | AC-17、20～21 | P-02、P-03、P-04、P-09、P-10、P-12、P-18～20 |
| 并发一致性、操作历史及迁移条件 | REQ-50、REQ-52、REQ-53 | AC-24、33、36～38 | P-02 固定机制；P-04～05、P-08、P-12～18 落实写入；P-19～20 校验读取；P-10 验证交付 |

单元验证主要证明时间、学期计算、状态转换和字段边界；真实目标 MySQL 的集成验证证明唯一性、事务和并发不变量；浏览器验证覆盖三个角色的完整流程。不得用内存数据库或单一串行页面演示替代 MySQL 并发证据。验收项可以分阶段累积证据，但其最终关闭责任由配套计划指定，不得因前置任务只完成一部分就标记全部通过。

| 不变量 | 主要证明 |
| --- | --- |
| INV-01～02 | AC-01～04、08、14、22～23、34～35 |
| INV-03～04 | AC-05～07、27 |
| INV-05～07 | AC-10～13、26、30～33、37 |
| INV-08 | AC-06、13、16、18、27、31～33 |
| INV-09 | AC-14～15、19、28、30～31、34～36 |
| INV-10 | AC-17、22～23、35 |
| INV-11～12 | AC-25～26、36 |
| INV-13～14 | AC-27～29、33 |
| INV-15 | AC-23～24、33 |
| INV-16 | AC-34～35 |

## 13. 决策结论与设计准入

### 13.1 原待定事项的正式结论

保留原 Q 编号用于追溯，不表示仍有业务问题等待用户确认。

| 编号 | 当前结论 | 状态与执行责任 |
| --- | --- | --- |
| Q-01 | 自助注册不填写编号，系统生成的登录账号同时为工号／学号；管理员建号仍可维护资料编号 | 已采纳，P-04、P-12 落实 |
| Q-02 | 受控演示、自助师生注册和管理员建号；真实身份校验在范围外 | 已采纳，不阻塞首版设计开发 |
| Q-03 | 姓名与描述必填、字段长度与唯一范围、至少一次授课、半小时对齐均按正文执行 | 已采纳，相关表单和服务端必须一致 |
| Q-04 | 课程编辑删除上下架、退课和换课均在范围内 | 已确定，P-13～17 实施 |
| Q-05 | 指定技术栈保持原值；实际可获取性和兼容组合不得伪称已验证 | 技术验证任务 P-02，完成后才初始化业务工程 |
| Q-06 | 逻辑删除、版本控制、重放与事务的业务结果已固定；具体数据及接口实现集中设计 | 技术设计任务 P-02，禁止分散到各实现任务临时选择 |
| Q-07 | 容量、性能、错误率和可访问性目标不再待确认；测量环境和限流配置需明确 | P-02 固定测试方案与阈值，P-10 实测 |
| Q-08 | 固定学年双学期、Asia/Shanghai、84 天半开区间及结束只读 | 已采纳，P-11 及各写入任务落实 |
| Q-09 | 初始未上架、下架保留、已有选课时不能删课或改时间；未结束可选退换 | 已采纳，P-08、P-13～17 落实 |
| Q-10 | 账号和角色不可编辑；逻辑删除、唯一值不复用、历史保留 | 已采纳，P-12、P-18 落实 |
| Q-11 | 空库按新版初始化；实际接入旧库时须显式映射而非推测 | 条件规则已采纳，P-02、P-10 检查适用性 |
| Q-12 | 任务计划已与正式需求同步 | 已完成，使用 development-plan.md v1.0.6 |

### 13.2 开发准入与设计交付

业务规格已经定稿，P-01 不再发起默认规则确认。P-02 和 P-03 可基于本文件直接启动设计；业务代码任务在以下交付完成后按依赖启动：

1. P-02 交付版本兼容证据、技术设计、接口与数据契约、并发/重试方案、验证命令及测试环境定义。所有会改变行为、接口、数据或证明的实现选择在此处解决。
2. P-03 在根目录 interaction-design.md 交付三角色页面流程、低保真示意、字段与状态、学期选择及周课表交互方案，并与根目录 design.md 的 P-02 技术契约一致；文档走查与实际视觉、浏览器验证分别记录。
3. 两项设计交付引用 requirement.md v1.0.6，记录自身版本；实现任务引用对应定稿版本，不自行改写规则。
4. 若固定技术版本实际无法获取或兼容，P-02 提供证据和最小变更建议，先更新技术约束再开始受影响实现；不能默默换框架版本。若只是选择满足原下限的更高 Node.js/Java 版本，按兼容证据记录即可。
5. 不要求为了进入设计或落实已授权规则再次取得用户确认；只有实际出现超出已确认范围的业务变更才走变更流程。

“正式需求”表示业务与验收基线已经确定，不表示系统已实现、设计已完成或测试已通过。

## 14. 变更控制

需求变化必须先记录涉及的 REQ、INV、AC，再同步设计、任务和验收。实现时发现技术方案不可行，应先补充设计；不能通过隐式更改业务约束来让实现通过。

| 版本 | 日期 | 变更 | 当时状态 |
| --- | --- | --- | --- |
| 0.1 | 2026-09-04 | 从草稿扩充业务、异常、数据和验收，并提出默认规则 | 历史草案 |
| 0.2 | 2026-09-04 | 加入管理员、学期、课程管理、退换课和教师学生查询 | 范围确定、默认规则尚待采纳 |
| 1.0 | 2026-09-04 | 用户采纳全部默认规则；形成正式业务及验收基线，同步任务计划 | 正式版 |
| 1.0.1 | 2026-09-04 | 用户逐项确认选择 A：采用锁内判定时刻；统一 REQ-40、REQ-42、REQ-50、INV-12 和 AC-36，同步计划与设计 | 已确认；其余审查事项继续逐项处理 |
| 1.0.2 | 2026-09-04 | 用户确认配色选项 A：固定 12 色可复用，课程样式稳定，编号、名称和边框辅助区分；同步 REQ-25、REQ-28、AC-15 与设计、计划 | 已确认；视觉走查与其他准入工作未完成 |
| 1.0.3 | 2026-09-04 | 用户确认 Java 选项 A：统一采用 Java 21 LTS、release=21；在原 Java 17+ 下限内固定运行选择，同步 REQ-34 及 P-02 环境验证要求 | 已确认版本路线；安装及兼容验证未执行 |
| 1.0.4 | 2026-09-04 | 用户确认 MySQL 选项 A：新建独立 MySQL 8.4 开发实例，保留现有 8.0.27 及数据；开发与隔离测试固定同一补丁，同步 REQ-34 和 P-02 | 版本及隔离路线已确认；安装与连接验证未执行 |
| 1.0.5 | 2026-09-04 | 用户确认 Docker 选项 A：Docker Desktop、WSL 2 和 Linux 容器；开发数据库独立持久化，Testcontainers 管理临时测试实例，同步 REQ-34 与 P-02 | 方案已确认；安装及运行验证未执行 |
| 1.0.6 | 2026-09-04 | 用户确认交互文档选项 A：独立 interaction-design.md，design.md 保留唯一技术契约；同步设计引用与任务交付路径，保持原 REQ、INV、AC 编号 | 交互文档已交付；环境与视觉验证未完成 |

### 14.1 本次定稿记录

- 采纳依据：用户明确要求采用 v0.2 全部默认规则形成适合开发的正式版，并同步任务计划。
- 业务内容：既有 53 项需求、16 条不变量、38 项验收标准全部保留，不新增或削减已确认功能。
- 文档变化：将草案和建议语气改为约束性表述，关闭业务待定项，分离技术设计执行事项，修复验收表格连续性，更新任务追踪。
- 计划变化：保留 P-01～P-10 的原任务主题，P-01 记为本轮完成；新增 P-11～P-20，覆盖此前缺少的业务能力。
- 交付边界：本次修改 requirement.md 与 development-plan.md，不执行计划中的工程初始化、业务实现、迁移或部署。
