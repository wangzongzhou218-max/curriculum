# SDD（规格驱动开发）：详细流程和核心思想

## 什么是 SDD

SDD（Spec-Driven Development，规格驱动开发）是一种把“规格”放在开发流程中心的方法：

> 先把要解决的问题、系统行为和验收标准定义清楚，再由规格推导设计、任务、测试和代码。

它不是简单的“先写一份需求文档”。真正的区别是：规格不再是开发前写完就丢弃的材料，而是整个项目持续维护的控制面和事实来源。

在 AI 辅助开发中，这尤其重要。与其不断向编码代理发送零散提示，不如让代理依次读取稳定、可审查、版本化的规格、计划和任务。GitHub Spec Kit 将其概括为：

```text
原则 → 规格 → 技术计划 → 任务 → 实现 → 验证
```

其核心工作流是 `Spec → Plan → Tasks → Implement`，并辅以澄清、检查清单和跨文档一致性分析。[GitHub Spec Kit](https://github.com/github/spec-kit)

需要注意，SDD 目前更像一组正在形成的方法论，而不是只有一种权威实现。不同团队对“规格必须驱动到什么程度”有不同理解。

## 核心思想

### 1. 先定义“什么和为什么”，再决定“怎么做”

规格阶段关注：

- 谁遇到了什么问题
- 系统必须表现出什么行为
- 什么结果才算成功
- 哪些内容不在本次范围内
- 有哪些业务、性能、安全和合规约束

技术栈、数据库和模块结构通常留到计划阶段。

这样可以避免一个常见错误：在业务需求尚未明确时，团队就开始围绕某种框架或数据库设计解决方案。

### 2. 规格必须可验证

“体验要好”“响应要快”“支持大量用户”不是合格的规格，因为无法客观判断是否完成。

更好的写法是：

- 在正常网络条件下，搜索结果的 P95 响应时间不超过 500ms。
- 当用户连续五次登录失败时，系统应将账户锁定十五分钟。
- 当订单支付成功后，用户刷新页面仍能看到已支付状态。

行为规格经常使用 Given–When–Then：

```text
Given 用户的购物车中有一件库存商品
When 用户成功完成支付
Then 系统创建订单
And 扣减对应库存
And 向用户发送确认通知
```

这种“以实例表达规格”的做法能够同时服务于需求沟通和验收测试，但它不能完全取代其他需求分析方法。[Specification by Example](https://martinfowler.com/bliki/SpecificationByExample.html)、[Given–When–Then](https://martinfowler.com/bliki/GivenWhenThen.html)

### 3. 规格是活的，而不是一次性文档

需求发生变化时，不应该只修改代码。正确顺序是：

```text
变更规格
  ↓
分析对设计、接口和任务的影响
  ↓
修改计划和测试
  ↓
修改实现
  ↓
重新验证
```

如果代码变了而规格没有变，规格就会失去可信度；如果规格变了而测试和代码没有同步，系统也不再符合规格。

### 4. 每一层都应该可追踪

理想状态下，可以从一条业务需求一路追踪到实现和测试：

| 层次 | 示例 |
|---|---|
| 业务目标 | 减少用户因重复提交导致的重复订单 |
| 规格 | 相同幂等键只能创建一个订单 |
| 技术设计 | API 接收 `Idempotency-Key` 并持久化处理结果 |
| 实现任务 | 添加幂等记录表和订单服务检查逻辑 |
| 自动测试 | 并发发送两个相同请求，只产生一个订单 |
| 监控指标 | 重复请求拦截数、重复订单数 |

这种追踪关系使团队能够回答：“为什么有这段代码？”以及“这条需求被哪些测试覆盖？”

### 5. 逐层细化，而不是一步生成全部代码

SDD 强调多轮收敛：

```text
模糊意图
→ 可审查的产品规格
→ 可执行的技术设计
→ 有依赖关系的原子任务
→ 小批量实现
→ 验证结果
```

每一层只解决该层的问题，并设置质量门槛。如果规格仍然有关键歧义，就不应该直接进入编码阶段。

### 6. 人负责意图和决策，AI 负责加速推导与执行

AI 可以帮助生成规格、发现遗漏、拆分任务和实现代码，但以下事项不应由 AI 静默决定：

- 产品范围
- 风险接受程度
- 安全和隐私边界
- 不可逆的数据迁移
- 架构上的长期承诺
- 需求冲突时的优先级

SDD 的价值不是“让 AI 自动写更多代码”，而是让 AI 的工作受到明确上下文、约束和验收条件控制。

## 详细流程

### 阶段 0：建立项目原则

在具体功能之前，先定义长期有效的工程约束，也可称为项目“宪章”：

- 支持的技术栈和运行环境
- 代码质量要求
- 测试覆盖策略
- 安全和隐私原则
- API 兼容性规则
- 性能基线
- 可访问性要求
- 日志和可观测性要求
- 禁止使用的依赖或架构模式
- 什么操作必须人工审批

例如：

```text
所有外部 API 必须有超时和重试上限。
任何包含个人信息的字段不得写入普通日志。
数据库结构变化必须提供向前迁移和回滚方案。
每个用户故事必须能独立测试。
```

这些原则会约束后续规格、设计和实现，而不是每次重新讨论。GitHub Spec Kit 将这一产物称为 `constitution.md`。[Spec Kit 工作流](https://github.com/github/spec-kit/blob/main/docs/index.md)

### 阶段 1：问题发现与范围界定

首先回答：

- 用户是谁？
- 当前流程有什么问题？
- 为什么现在要解决？
- 成功如何衡量？
- 哪些利益相关者会受到影响？
- 已知约束是什么？
- 本次明确不做什么？

建议形成：

- 问题陈述
- 目标
- 非目标
- 成功指标
- 风险和假设
- 术语表
- 未解决问题列表

“非目标”尤其重要，因为它能抑制开发者或 AI 代理过度扩展范围。

### 阶段 2：编写功能规格

一份较完整的功能规格通常包括：

1. 背景和目标
2. 用户角色
3. 用户故事或主要场景
4. 功能需求
5. 验收标准
6. 异常和边界情况
7. 非功能要求
8. 外部依赖
9. 数据和隐私要求
10. 不在范围内的内容
11. 未决问题

示例：

```text
需求 R-03：取消订单

已付款但尚未发货的订单可以由购买者取消。

验收标准：
- 未付款订单可以直接取消。
- 已付款且未发货的订单取消后必须进入退款流程。
- 已发货订单不能由用户自行取消。
- 重复提交取消请求不得产生重复退款。
- 用户必须能看到取消或退款失败的明确原因。
```

这一阶段尽量不写“使用 Redis 实现幂等”，因为那是设计决策，不是用户可观察的需求。

### 阶段 3：澄清与规格评审

逐条检查：

- 是否存在“快速、友好、合理”等不可测量词语？
- 每个主体和状态是否定义清楚？
- 权限不足、超时、重复请求、并发冲突等情况是否覆盖？
- 功能需求之间是否矛盾？
- 是否遗漏性能、安全、审计或可访问性要求？
- 每项需求是否能写出验收测试？
- 需求优先级是否明确？
- 是否有未经确认的假设？

质量门槛是：参与者对系统应该表现出的行为达成一致，而不只是“文档已经写完”。

### 阶段 4：制定技术计划

规格回答“做什么”，技术计划回答“怎么实现”。

典型内容包括：

- 现有系统分析
- 架构和模块边界
- 数据模型
- API 或事件契约
- 状态转换
- 权限模型
- 第三方依赖
- 迁移和兼容方案
- 错误处理
- 测试策略
- 可观测性
- 发布与回滚方案
- 备选设计及取舍

技术计划必须反向引用规格。例如，“订单幂等表”应当对应具体的重复提交需求，而不是因为设计者觉得将来可能有用。

计划阶段还要防止两类问题：

- **欠设计**：只写“增加一个接口”，没有说明数据一致性、安全或迁移。
- **过度设计**：为一个简单功能引入微服务、消息队列或通用框架，却没有规格依据。

### 阶段 5：拆分实现任务

把技术计划拆成小而可验证的任务。好的任务应具有：

- 明确目的
- 明确输入和输出
- 明确文件或模块范围
- 明确依赖
- 明确完成条件
- 对应的测试或验证方式

应优先按可独立交付的用户故事纵向切分，而不只是：

```text
先完成全部数据库
再完成全部后端
最后完成全部前端
```

更理想的是：

```text
第一批：用户可以创建订单
第二批：用户可以查看订单
第三批：用户可以取消未发货订单
```

每一批都包含必要的数据、接口、界面和测试，能形成独立的验证闭环。

### 阶段 6：一致性和覆盖分析

编码前进行一次跨产物检查：

- 每条需求是否至少有一个实现任务？
- 每个任务是否能追溯到某条需求或工程约束？
- 是否有需求没有验收标准？
- 是否有任务偷偷扩大产品范围？
- 计划中的接口、数据模型和任务是否一致？
- 安全、性能和迁移要求是否落实到任务？
- 任务顺序是否符合依赖关系？

可以建立简单的需求覆盖矩阵：

```text
R-01 → T-01、T-02 → Test-01
R-02 → T-03       → Test-02、Test-03
R-03 → T-04、T-05 → Test-04
```

无法追踪的需求和任务都应受到质疑。

### 阶段 7：小批量实现

实现阶段按照任务依赖顺序推进：

1. 先读取对应规格和技术计划。
2. 编写或更新测试。
3. 实现最小必要改动。
4. 运行局部测试和静态检查。
5. 检查是否违反项目原则。
6. 记录与计划不同的地方。
7. 完成一个小批次后再继续。

实现中发现规格错误时，不应默默在代码里“自行修正”。应返回规格阶段更新决策，再同步计划、任务和测试。

### 阶段 8：验证与验收

验证至少分为四层：

- **实现正确性**：单元测试、集成测试、类型检查、静态分析。
- **需求符合性**：逐条执行验收标准。
- **非功能质量**：性能、安全、稳定性、可访问性。
- **真实环境表现**：浏览器行为、部署配置、数据库迁移、日志和监控。

代码测试全部通过并不一定等于验收通过。例如，单元测试可能验证了退款函数，却没有验证重复请求是否触发两次真实支付渠道退款。

### 阶段 9：交付并同步规格

交付时同步更新：

- 规格状态
- 验收结果
- API 契约
- 架构决策记录
- 数据迁移说明
- 运维手册
- 已知限制
- 后续事项

最终应确保“规格描述的系统”和“实际运行的系统”仍然一致。

### 阶段 10：变更与迭代

新的需求从规格差异开始，而不是直接编辑代码：

```text
旧规格 + 变更请求
→ 规格差异
→ 影响分析
→ 新计划和任务
→ 实现与回归验证
```

大型功能不宜塞进一个巨大规格。可以先建立路线图，再将其拆成多个能够独立经历 `规格 → 计划 → 任务 → 实现` 循环的子功能。[Spec of Specs](https://github.github.com/spec-kit/concepts/spec-of-specs.html)

## SDD 与其他开发方法的关系

| 方法 | 主要问题 | 与 SDD 的关系 |
|---|---|---|
| PRD | 产品要解决什么问题 | 可作为规格输入，但通常不足以直接驱动实现 |
| BDD | 系统在具体场景中应如何表现 | 很适合编写 SDD 的行为和验收标准 |
| TDD | 如何用测试推动代码设计 | 通常发生在 SDD 的实现阶段 |
| ADR | 为什么选择某个架构方案 | 保存技术计划中的重要决策 |
| 瀑布模型 | 按阶段顺序完成整个项目 | SDD 不必是瀑布式，可以按小功能反复循环 |
| 敏捷开发 | 如何以增量方式交付价值 | SDD 可以作为每个故事或迭代内部的开发纪律 |
| Vibe Coding | 通过连续提示快速生成代码 | SDD 用稳定规格替代临时、不可追踪的提示上下文 |

最容易产生的误解是“SDD 就是更重的瀑布文档”。实际上，好的 SDD 规格应该短周期迭代、版本化并随着系统演进。对当前一些 SDD 工具的批评恰恰是：它们有时只能做到开发前的 spec-first，却没有真正做到长期的 spec-anchored，即系统变化后仍以规格为锚点。[Martin Fowler：当前 SDD 工具分析](https://martinfowler.com/articles/exploring-gen-ai/sdd-3-tools.html)

## 常见失败模式

- **把规格写成愿望清单**：没有边界情况和验收标准。
- **追求虚假的完备性**：文档很长，但关键决策仍然模糊。
- **把技术方案混进产品规格**：尚未分析就过早锁定实现。
- **规格规模过大**：代理或开发者无法稳定维持完整上下文。
- **未经人工审核就接受 AI 规格**：看似完整，实际包含错误假设。
- **只在项目开始时维护规格**：代码演进后规格迅速失真。
- **任务与需求脱节**：做了很多工程工作，却无法解释服务于什么目标。
- **把测试通过等同于用户验收**：实现正确，但产品行为不符合真实需求。
- **所有项目都使用同样重量的流程**：小型实验也被大量文档拖慢。

## 什么时候值得使用

比较适合：

- 需求复杂或涉及多个角色
- 多人、多团队或多个 AI 代理协作
- 有安全、隐私、合规要求
- 接口和数据契约较多
- 需要长期维护
- 改造遗留系统
- 失败成本较高
- 需要明确审计和决策记录

可以简化甚至跳过正式流程：

- 一次性脚本
- 探索性原型
- 需求尚处于快速试错期
- 极小且低风险的修改

即使是小功能，也可以采用最小版本：

```text
目标与非目标
→ 3～5 条可测试需求
→ 简短技术方案
→ 任务清单
→ 实现与验收
```

一句话总结：**SDD 不是为了写更多文档，而是为了让产品意图、工程决策、任务、测试和代码形成一条可追踪、可审查、可持续更新的链路。**
